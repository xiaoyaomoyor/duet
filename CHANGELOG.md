# 变更日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.9.0] — 2026-09-20 · R4 多方对比

- 支持 2—6 个对象，提供添加、改名、排序、移除及完整撤销。A—F 标记绑定稳定身份，排序不改变作品归属，共同内容不因移除首个对象而丢失。
- 新增三方横排、四方 2×2、五至六方 3×2 总览；支持固定参照的重点双人、单项与全体切换。六方总览在 1080p／720p 完整展示，窄屏纵向阅读。
- 多方数据表按宽度分组，重复维度与固定参照；阅读报告采用可读的多列布局。剧场只挂载当前场景和观看对象，切换时暂停媒体。
- 工程 schema 升至 10，保留已有恢复快照；schema 9 无快照工程保存完整原作品结构，文件与数据库迁移共用校验路径。
- 离线 HTML 支持六方独立作品选择与观看方式，以按对象组合渲染片段取代多方组合枚举；媒体去重，保持匿名和步骤语义。
- 579 项单测、100 项浏览器测试通过。使用方式与截图见 [R4 验收记录](docs/07-R4多方对比验收.md)。

## [0.8.0] — 2026-09-20 · R3 多作品与演示编排

- 新增多测试题、单工具多作品、批量音频／图片导入、缩略图与搜索；每份作品统一保存媒体、歌词、评分和观察，支持独立选择、默认作品和对比组合。
- 新增场景引用与四类步骤，支持复制、隐藏、排序、前进／回退、首尾跳转、重新开始、身份揭示及工具聚焦。演示选择和步骤不写入工程或撤销历史。
- schema 升至 9：统一 v1—v8 文件／数据库迁移，保留升级前恢复工程；完整校验引用，复制时重建实体标识，资源遍历覆盖未激活作品和快照。
- 新增含作品切换与步骤的离线单文件网页，同一舞台渲染器输出，媒体去重且匿名输出不携带身份映射；超出 120 个画面组合或 80 MB 时可改选静态阅读页。
- PNG 支持当前步骤／完整场景／阅读报告，导出后保留演示状态。更换音频时提示复核保留歌词；PWA 更新避开演示并等待编辑保存。
- 修复旧播放器因数据对象更新误清时长和同步状态，以及工程复制时 ID 与结构字段同名的引用问题。
- 使用方式、实际画面和验收范围见 [R3 验收记录](docs/06-R3多作品与演示编排验收.md)。

## [0.7.0] — 2026-09-19 · R2 双方舞台工作区

- 新增音乐、图片、通用评测模板，正式接入项目编辑、自动保存、撤销与重做。左侧内容结构、中间成稿预览、右侧内容／样式属性，复杂编辑可展开统一窗口。
- 将 R1 的墨色／纸白、试听舞台和对齐数据表用于真实工程。参数按维度名称对齐，保留零值与缺项；项目风格独立于工作区主题保存。
- 提供阅读报告、16:9 剧场、干净画面、全屏、方向键切换及分层 Escape。空／隐藏内容不进入演示，过长内容展开阅读，手机纵向呈现。
- 试听默认独听；切场景、进入演示、导出和离开页面时暂停。稳定音频宿主避免编辑／演示切换重建播放器，保留封面和播放条显示选项。
- PNG 支持当前场景／完整报告，只读 HTML 复用完整报告并保留离线原生媒体控件，`.duet` 往返保存项目主题。修复 PNG 居中外边距导致偏移裁切、项目与工作区主题不同时背景取色错误。
- 保留兼容画布及原有配色；旧项目可“另存舞台副本”，原稿不变。使用 schema 8 适配默认测试题／样本，完整多样本迁移仍属 R3。
- 修复直接访问项目链接时被“恢复上次位置”覆盖的问题；导入后自动打开项目。更新模块弹窗布局与全局控件色彩。
- 完成 557 项单测、85 项浏览器用例、构建、PWA／体积／对比度检查；实际画面与范围说明见 [R2 验收记录](docs/05-R2舞台工作区与验收.md)。

## [0.6.0] — 2026-09-17 · R1 试听舞台与统一视觉样板

- 新增“展示样板”入口及 `#/showcase`：墨色／纸白双主题，命题、成对试听、媒体与观察、维度比较、结论五类场景。
- 建立统一色彩、系统字体、间距、按钮、表单与 S／M／L 窗口规范。以中性舞台、原创风景图、身份小标记和对齐比较表呈现“试听舞台＋数据排版”。
- 样板支持真实试听、定位、独听、本地音频替换、失败提示及临时文字编辑。使用原创合成音频，明确区分示例数据与真实平台评测。
- 提供 16:9 干净画面、方向键切换、Escape 返回、减少动效、空素材与长内容阅读布局；完成中英文和手机适配。
- 独立媒体宿主避免场景切换时重建；离场暂停并回收本地素材 URL。样板与已有项目快捷键隔离，工程结构仍为 `8`。
- 增加 13 项浏览器用例和 38 组主题对比度检查；设计规范、实际截图及性能测量见 [R1 验收记录](docs/04-R1视觉规范与验收.md)。现有项目的新界面接入属于 R2。

## [0.5.8] — 2026-09-17 · R0 可靠性与 Web 范围收口

- 音频播放入口与封面显示解耦：长条、正方形布局隐藏封面后仍可播放；关闭播放条的设置继续有效。无封面长条保留曲名、时长与完整宽度。
- 修复只读 HTML 丢失作用域样式、主题写死和音频不可播放的问题。导出等待异步模块与媒体解析，保留画布结构、背景、主题；音视频使用原生控件，媒体、背景及图标按选项内嵌。
- 匿名标题在导出中保留遮罩；摘要、图标替代文本与临时揭晓按匿名规则处理。长图和只读网页保持匿名，`.duet` 工程文件保留原始信息。
- 通用模块补齐输入标签，翻译占位文案和下拉选项。统一六类弹窗的初始焦点、Tab 循环、背景隔离、Escape 和焦点恢复，修复外链输入框 Enter 重复提交。
- 移除 Tauri 源码、原生文件桥接、平台探测和桌面图标生成路径；继续支持 Web 与 PWA。
- 应用版本、npm 清单与锁文件统一为 `0.5.8`；工程结构版本仍为 `8`，无需迁移现有数据。
- 新增导出、匿名、键盘和媒体回归测试。详见 [R0 验收记录](docs/03-R0验收记录.md)。

## [0.2.0] — 实测反馈修订

按真实使用反馈（Suno / Lyria 对比实测）做的第二轮大改，重点是**编辑体验**与**可读性**。

### 编辑方式重做

- **卡片就是最终效果**：编辑视图里的模块卡片与展示视图共用同一个呈现组件，
  因此"编辑时看到的样子"确实是"成稿的样子"；额外只多出右上角四个按钮。
- **模块编辑弹窗**：标题、正文、呈现选项都收进弹窗，编辑视图不再需要一块"预览区"。
- **工具卡片重做**：强调色条移到左侧、LOGO 放大、名称加大居中、版本紧随其后。
- **终于能选工具了**：此前 `side.toolRef` 只被读取、没有任何入口可改——
  现在可以从工具库挑选生产源，并自定义名称 / 版本 / 备注 / 图标，逐项控制显示什么。

### 排版可调

- 拖行间分界线调**行高**、拖中轴调**左右列宽**、拖侧栏边缘调**侧栏宽度**；
  都能用方向键微调、双击恢复默认，并随 `.duet` 工程文件保存。
- 数据结构升到 **v2**（新增行高与工具卡片显示开关），带迁移；v1 工程文件仍可正常打开。

### 媒体

- **音频自动读取 mp3 内嵌封面**（零依赖 ID3v2 解析，支持 v2.3 / v2.4 与 UTF-16 描述串）。
- **歌词支持把 `.txt` / `.lrc` 直接拖进来**（此前只有按钮可用），
  拖入非文本文件会明确说明原因而不是静默忽略；同时新增对齐方式设置。

### 主题与图标

- **四套主题**：跟随系统 / 暗 / 亮 / 紫夜，默认跟随系统并实时响应系统深浅色切换。
- 修复紫夜主题里"部分文字看不清"：对比度校验脚本改为**直接解析 `tokens.css`**，
  立刻查出 4 项此前看不见的不达标（最严重的是弹窗背景上的次要文字只有 4.49:1）。
- **图标全部换成 [Lucide](https://lucide.dev)** 并按语义重新分配——
  此前"属性面板"与"设置"共用同一个齿轮图标。

### 外壳

- 删除顶栏的全局搜索框与标签栏的"新建"按钮（左侧项目列表已有同样入口）；
  `Ctrl/Cmd + K` 保留，改为直接打开命令面板。
- 侧栏宽度可拖拽调节。

### 工具库与合规

- 工具库加入**免责声明**：名称与图标归各自所有者，不代表官方关联或背书。
- **仓库不内置任何第三方商标**。想在本机使用真实 LOGO 可运行 `npm run logos`，
  文件写入已被 `.gitignore` 排除的目录，只影响你自己的实例。

### 修复

- **模板注释里的连续短横线曾让长图导出彻底失败**：导出会把 DOM 序列化成 SVG，
  而 XML 不允许注释内出现连续两个短横线。现在导出会剔除所有注释节点兜底，
  错误信息也从无意义的 `[object Event]` 变成可读的资源地址。
- **主题"怎么选都选不动"**：设置守卫写成"与默认值不同就重置"，多主题下会打回默认值。
- **音频封面永远不显示**：把音频自身的资源 id 传给了图片组件。
- 另有拖拽调宽后刷新又变回去、Esc 关不掉弹窗导致遮罩拦截点击、
  空模块标题有两条 DOM 路径、品牌图标把工具名挤成省略号等。

### 工程

- 单元测试 **449**；端到端 **54**（全部通过）。
- 校验脚本：编码扫描、对比度（**逐主题** 3 × 48 项）、PWA 产物、首屏体积。

[0.2.0]: https://github.com/xiaoyaomoyor/duet/releases/tag/v0.2.0

## [0.1.0] — 首个可用版本

第一个"可以拿去用"的版本：五个里程碑全部落地，覆盖从录入、对比、演示到分发导出的完整链路。

### 对比能力

- **23 种对比模块**：封面、图片、图集、音频、视频、3D 模型、歌词、进度条、文字、Markdown、
  富文本、备注、参数表、链接、评分条、星级、标签组、时间线、代码块、代码对比、网页嵌入、分割线、占位块。
- **模块可任意改名、重复添加**：把"文字"改成"价格"或"简评"即可复用同一种模块表达不同维度。
- **空模块自动隐藏**：没填内容的模块在展示视图不出现，整行皆空时整行跳过，全空时给出引导而非白屏。
- **左右两方可以是任何东西**：内置 47 个常用 AI 工具，也可自建"生产源"并上传图标。

### 编辑与演示

- **双视图**：编辑视图配置内容；展示视图只读、动效全开，适合录屏与对外演示。
- **撤销重做**：基于整项目快照，配合 800ms 合并窗口与 100 步深度上限，连续输入不会产生几十步撤销。
- **实时预览**：每张模块卡片下方直接用展示视图的渲染器输出效果。
- **拖拽排序**：行内模块顺序、行的上下顺序均可拖拽调整。
- **统一动效系统**：14 类动效全部走 CSS token，`prefers-reduced-motion` 与设置项双重降级。

### 音频

- **双轨同步播放**：两条音轨共用一个 `AudioContext` 时钟，250ms 采样、120ms 阈值漂移校正。
- **Solo / Mute**：通过 gain 淡出实现，不暂停音轨，避免重新起播带来的对齐误差。
- **真实频谱**：由 `AnalyserNode` 驱动，按侧独立；全局仅一个 rAF 循环，页面不可见时自动停表。

### 数据与导出

- **本地优先**：全部数据在浏览器 IndexedDB，无账号、无服务器、无上传。
- **`.duet` 工程文件**：可选内嵌媒体；导入先落媒体再落项目，缺失媒体如实上报而非静默丢弃。
- **长图 PNG**：导出前等待字体与图片就绪，外链图片自动内联，超尺寸自动降倍率。
- **只读 HTML**：单文件、无运行时、剔除全部可交互元素。
- **备份与恢复**：一键导出全部数据；从备份恢复时生成副本，不会覆盖现有同名项目。
- **存储面板**：配额可视化、持久化存储授权、未引用媒体清理。

### 分发

- **PWA**：可安装到桌面，安装后离线可用；新版本以提示方式询问，不静默接管正在编辑的页面。
- **Tauri 2 脚手架**：`src-tauri/` 配置、`fetch_remote` 原生命令（绕开 CORS）、单实例、
  `.duet` 文件关联、PNG/ICO/ICNS 全套图标、`platformAdapter.ts` 平台适配层。
  ⚠️ **未构建验证**：开发机没有 Rust 工具链，这部分代码没有编译过。

### 工程质量

- **零运行时依赖的"自研件"**：LRC 解析、Markdown 渲染、HTML 白名单净化、
  glTF/GLB 解析、WebGL 静态模型查看器、行级文本对比（LCS）、PNG/ICO/ICNS 图标生成。
- **首屏体积**：JS 111.2 KB gzip（预算 260）、CSS 5.1 KB gzip（预算 40）。
- **校验脚本**：编码扫描、WCAG AA 对比度、PWA 产物、首屏体积全部接入 `npm run check` / `npm run build`。
- **测试**：单元测试 414 条、端到端测试 54 条。

### 安全

- Markdown 与富文本**先整体转义、再做语法替换**，顺序写反即构成 XSS，测试中有专门用例锁定。
- 富文本走白名单净化：白名单标签先替换为不可伪造的内部占位符，其余尖括号一律转义。
- 外链媒体抓取有超时与体积上限；桌面版原生命令只允许 `http`/`https`。
- 网页嵌入的 sandbox 不含 `allow-same-origin`，嵌入页无法访问本应用数据。

[0.1.0]: https://github.com/xiaoyaomoyor/duet/releases/tag/v0.1.0
