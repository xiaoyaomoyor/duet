# 对奏 Duet · 施工总案（Construction Master Plan）

> **对外名称**：**对奏 / Duet**（✅ 已定稿，2026-09-15）
> **本地目录 / 仓库名 / 代码标识**：`Duet` / `duet` / `duet`
> **开源仓库**：https://github.com/xiaoyaomoyor/duet
> **开源协议**：**AGPL-3.0**（见 §20 Q6）
> **文档版本**：v1.1
> **状态**：✅ 评审通过，M0 已完成（见 §17）
> **文档定位**：这是本项目的**唯一施工依据**。后续所有开发、评审、验收都以本文档为准；实现与文档冲突时，先改文档再改代码。

---

## 0. 阅读指引

| 你是谁 | 先读 | 再读 |
| --- | --- | --- |
| 产品/需求方 | §1 §2 §3 §4 | §9 §10 §16 |
| 前端开发 | §5 §6 §7 §8 | §11 §13 §14 §19 |
| 测试/验收 | §16 §18 | §7 §10 |
| 后续接手者 | 全文 | §20 §21（决策记录与待决清单） |

**术语表**（全文统一，禁止混用同义词）：

| 术语 | 英文/标识 | 含义 |
| --- | --- | --- |
| 工具 | Tool | 被对比的"生产源"，如 Suno、可灵、Meshy。含内置工具与自定义工具 |
| 对比项目 | Project | 一个可保存、可切换的完整对比工程（Obsidian 的"笔记"） |
| 对比页 | Sheet | 一个对比项目内的**一屏对比**（v1 一个项目 = 一张对比页，v2 支持一个项目多张） |
| 侧 / 方 | Side | 对比中的一方（左 / 右）。v1 固定 2 侧，v2 支持 N 侧 |
| 行 | Row | 对比中的一行，包含左格与右格（或一个整宽格） |
| 模块 | Module | 可增删的内容单元（封面、歌词、音频、视频、文字…），有类型 + 自定义标题 + 内容 |
| 模块类型 | ModuleType | 模块的"类"，注册在模块注册表中（`cover`、`lyrics`、`audio`…） |
| 模块实例 | ModuleInstance | 某个格子里放的一个具体模块，带自己的标题、配置、内容 |
| 资源 | Asset | 用户导入或被镜像到本地的媒体文件（图片/音频/视频/文本） |
| 展示视图 | Present | 只读、带完整动效的对外展示态 |
| 编辑视图 | Edit | 可填写、增删、调参的编辑态 |

---

## 1. 命名决策记录（ADR-0001）

### 1.1 候选评估

命名采用 **中文名（对外传播）+ 英文标识（仓库/域名/npm/CLI）** 双轨制。

| 候选 | 英文标识 | 评估 |
| --- | --- | --- |
| **对奏** | `duet` | 「对」= 对立、对质、对着弹；「奏」= 演奏。**同时含"对唱"与"对擂"**，一个"对"字把"和"与"比"说尽。两字、上口、Logo 可做成两段波形面对面 |
| 二重奏 | `duet` | 音感最好，但只有"和"没有"比"，描述协作而非对抗，**已否决** |
| 较场 | `joust` | 谐音「校场」= 比武校阅之地，「较」直接=比较、较量。评测竞技场气质最强 |
| 双镜 | `twinlens` | 两台镜头对准同一事物，同框即对比。视觉系统好做 |
| 并奏 | `duet` | 两声部同时进行、当场比对，比"二重奏"多一层横向张力 |
| 比物 | `paragon` | 出自"比物丑类"，古典路线，字面直接就是"比" |
| 格物 | `gewu` | "格物致知"，文化厚度最高，但与"对比"的关联需一句副标题解释 |
| 万物对比 | `versus` | 最直白的品类名，但无商标性与辨识度，SEO 也弱 |
| 对台 | `faceoff` | 唱对台戏，最好懂但偏随意；`FaceOff` 已被 LLM benchmark 占用 |

### 1.2 最终结论（已定稿）

> **中文「对奏」，英文标识 `duet`，本地目录 `Duet`，仓库名 `duet`，协议 AGPL-3.0。**

品牌描述句（对外统一使用）：

> **对奏 Duet — 让两个工具，同台演奏同一道题。**
> *Same prompt. Two tools. One stage.*

选它的核心原因：「对」同时含"对唱"与"对擂"——**一个"对"字把"和"与"比"说尽**。
这是"二重奏"做不到的：二重奏只有"和"，描述的是协作而非对抗。

### 1.3 全局替换约定（保留）

名称已定稿，但下列约定继续有效，用于降低未来任何改名成本：

1. 代码中**禁止硬编码产品名**。所有展示名从 `src/app.config.ts` 读取：
   ```ts
   // src/app.config.ts
   export const APP = {
     nameZh: '对奏',
     nameEn: 'Duet',
     slug: 'duet',
     fileExt: 'duet',          // 工程文件扩展名 .duet
     dbName: 'duet-db',
     repo: 'https://github.com/xiaoyaomoyor/duet',
   } as const
   ```
2. 工程文件扩展名为 **`.duet`**；若未来改名，只需改 `fileExt` 与 `.duet` 相关的 3 处常量 + 文档全局替换。
3. `[to-be-replaced]` 标记已全部清除——文档中不再存在任何占位名。

---

## 2. 产品定位

### 2.1 一句话定位

> 一个**专注内容横向对比**的本地优先 Web 应用：把两个工具在**同一套输入**下的产出，按模块并置成一张可编辑、可展示、可导出的对比页。

### 2.2 形态对标

- **应用形态**对标开源项目「魔方简介」：单页应用、离线可用、双击即用、无账号体系、数据全在本地。
- **内容结构**对标 LLM 评测平台（LMArena / Artificial Analysis）：左右并置、分维度打分、可对外展示。
- **信息组织**对标 Obsidian：左侧项目列表 + 顶部标签页 + 工程文件可携带、可迁移。

### 2.3 与竞品/替代方案的差异

| 替代方案 | 它的做法 | 我们的差异 |
| --- | --- | --- |
| Excel / Notion 表格 | 纯文字罗列，媒体只能贴链接 | 媒体原生内嵌、可播放、有动效 |
| 剪辑软件（PR/剪映） | 能做对比视频，但过程重、不可复用 | 一次配置、随时切换编辑/展示、可直接导出长图与只读页 |
| LMArena 等在线评测站 | 只支持 LLM 文本，数据在别人服务器 | 支持音乐/视频/3D/音频/图片/代码任意内容，数据 100% 本地 |
| 手工截图拼图 | 灵活但极难维护、无法二次编辑 | 工程化：改一处，展示视图与导出同步更新 |

### 2.4 目标用户

| 用户 | 场景 | 核心诉求 |
| --- | --- | --- |
| AI 工具评测博主 / UP 主 | 制作「可灵 vs 即梦」对比视频 | 展示视图要好看、动效足、能直接录屏或导出长图 |
| 模型/工具选型者 | 团队内选型汇报 | 结论一目了然、可导出只读页分享给同事 |
| 提示词工程师 | 同一 prompt 跨模型跑结果 | 快速录入、批量建项目、模板复用 |
| 兴趣爱好者 | 对比两台相机、两款游戏、两个商品价格 | 零门槛、不用登录、双击就能用 |

### 2.5 非目标（明确不做，防止范围蔓延）

- ❌ 不做账号体系、不做云端同步、不做多人实时协作（v1）。
- ❌ 不做在线抓取/爬取第三方平台内容（用户自己提供素材）。
- ❌ 不做 AI 自动评分/自动生成结论（人来做判断，工具只负责呈现）。
- ❌ 不做移动端编辑（展示视图需在手机上可读，但编辑假定桌面 ≥1024px）。
- ❌ 不做服务端渲染与 SEO 站点（纯 SPA + 本地优先）。

---

## 3. 核心用户旅程

### 3.1 主旅程：做一次音乐对比

```
① 打开应用 → 底座为「项目列表 + 标签页 + 空状态引导」
② 点「新建对比」→ 选模板「音乐对比」
   → 自动生成：左侧 Suno、右侧 Lyria，含[封面][歌词][音频 Player][进度条]四行
③ 编辑视图逐行填写：
   - 点封面格 → 上传本地图片 → 自动生成缩略图
   - 点歌词格 → 导入 .txt / 直接粘贴 → 支持 LRC 时间轴
   - 点音频格 → 拖入 mp3 → 自动解析时长、生成波形
   - 双方主题色：左紫 / 右青（默认）
④ 点「展示视图」→ 只读、动效全开
   - 点「同步播放」→ 两首歌从同一时刻起播，进度条联动
   - 按 Esc 退出展示视图
⑤ 点「导出」→ 长图 PNG / 只读 HTML / .duet 工程文件
⑥ 自动保存已生效（防抖 300ms 落盘 IndexedDB）
```

### 3.2 关键体验红线（不可妥协）

| # | 红线 | 说明 |
| --- | --- | --- |
| R1 | **零配置可用** | 首次打开到能开始编辑 ≤ 2 次点击 |
| R2 | **永不丢数据** | 任何编辑在 500ms 内落盘；关闭标签页前强制 flush；崩溃后可恢复 |
| R3 | **展示视图绝对只读** | 展示态下不存在任何可输入元素，误触不改数据 |
| R4 | **空模块自然消失** | 未填内容的模块在展示视图默认不渲染，不留空白 |
| R5 | **左右可像素级对齐** | 同一行的左右两格顶部对齐、行高由内容撑开，不允许错位 |
| R6 | **媒体不阻塞 UI** | 导入 200MB 视频时界面不卡、有进度反馈 |

---

## 4. 功能范围与优先级

### 4.1 范围总表

| ID | 功能 | 优先级 | 里程碑 |
| --- | --- | --- | --- |
| F-01 | 多对比项目管理（项目列表 / 标签页 / 新建 / 重命名 / 复制 / 删除 / 搜索 / 排序） | P0 | M1 |
| F-02 | 编辑视图 ↔ 展示视图切换（含只读态隔离） | P0 | M3 |
| F-03 | 行/模块系统：任意增删、重复添加同类型、自定义标题 | P0 | M2 |
| F-04 | 模块内容：文字、封面图、图片集、歌词（含 LRC 时间轴）、音频、视频、进度条 | P0 | M2 |
| F-05 | 空模块自动隐藏 + 单模块手动隐藏开关 | P0 | M2 |
| F-06 | 每侧主题色（含一键配色方案） | P1 | M2 |
| F-07 | 媒体导入：本地文件（拖拽/选择）+ 外链 URL（含 CORS 兜底与镜像缓存） | P0 | M2 |
| F-08 | 工具（生产源）管理：内置工具库 + 自定义工具（名称 + 图标上传） | P0 | M2 |
| F-09 | 设置页：主题、语言、保持位置、对比界面配置、存储管理 | P0 | M1 |
| F-10 | 音频双轨同步播放（A/B 对比试听）+ 频谱动效 | P0 | M4 |
| F-11 | 展示视图动效系统（入场、中轴光带、播放脉冲、数字滚动、进度环） | P1 | M4 |
| F-12 | 导出：`.duet` 工程文件 / 长图 PNG / 只读 HTML | P1 | M3 |
| F-13 | 导入：`.duet` / JSON / 内置模板 | P1 | M3 |
| F-14 | 撤销重做 + 快捷键体系 | P1 | M2 |
| F-15 | 拖拽排序行、跨格移动模块、批量操作 | P2 | M2 |
| F-16 | 模板库（音乐/图片/视频/3D/代码/价格/通用） | P2 | M5 |
| F-17 | PWA 离线 + 安装到桌面 | P2 | M5 |
| F-18 | Tauri 2 桌面版（exe/msi） | P2 | M5 |
| F-19 | 更多模块：3D 模型查看、代码 diff、评分条、表格、Markdown、时间线 | P3 | M5 |
| F-20 | N 方对比（>2 侧） | P3 | v2 |
| F-21 | 一个项目多张对比页 | P3 | v2 |
| F-22 | 自由画布布局 / 拖拽调宽 | P3 | v2 |

### 4.2 明确排除（v1 不做）

账号、云同步、协作、评论、公开分享链接、在线采集、AI 评分、移动端编辑、服务端。

---

## 5. 技术选型（ADR 汇总）

| # | 选择 | 理由 | 备选与否决原因 |
| --- | --- | --- | --- |
| T1 | **Vue 3.5+（`<script setup>`）+ TypeScript(strict)** | 编辑视图是典型的"表单 + 列表增删 + 拖拽 + 响应式联动"，Vue 的细粒度响应式能显著减少手写 diff；生态组件（播放器、拖拽、虚拟列表）成熟 | 原生 DOM：富交互下样板代码过多；React：本项目无服务端渲染与复杂状态共享需求，收益不抵心智成本 |
| T2 | **Vite 6** | 冷启动快、HMR 稳、插件生态齐（PWA / Tauri / 单文件） | —— |
| T3 | **Pinia** | 官方状态方案；setup 语法 store；配合 `storeToRefs` 精准订阅 | 自研 store：省 2KB 但要重造 devtools 与热更新支持 |
| T4 | **Vue Router 4（hash 模式）** | 两个主界面 + 深链（`#/p/:projectId`）；hash 模式保证 `file://` 与 Tauri 下无需服务端改写 | history 模式：静态托管需 fallback 配置，`file://` 直接不可用 |
| T5 | **IndexedDB（手写 Promise 封装，零依赖）** | 唯一能存 Blob 大文件的浏览器存储；手写封装保持可控、可测、无供应链风险 | Dexie：功能强但增加依赖与学习面；localStorage：容量 5MB，存不了媒体，直接淘汰 |
| T6 | **`.duet` 工程文件（JSON，媒体可选内嵌）** | 可读、可 diff、可手工修复；导出即备份 | zip 包：需引入压缩库且不便 diff；纯 JSON 内嵌 base64：大项目膨胀 33% |
| T7 | **`vue-draggable-plus`** | 基于 SortableJS，Vue 3 原生维护（`vuedraggable` 已停止对 Vue 3 的有效维护） | 自研拖拽：触摸/键盘无障碍成本高 |
| T8 | **`html-to-image`** | 长图导出效果优于 html2canvas（保留滤镜、CSS 变量、SVG） | html2canvas：对新 CSS 支持滞后，圆角阴影易失真；需按 §8.6 做健壮性加固 |
| T9 | **Web Audio API（`AudioContext` 单一时钟）** | 双轨同步播放必须共用一个时钟源，`<audio>` 各自为政必然漂移 | 纯 `currentTime` 对齐：漂移可达数百毫秒，音乐对比场景不可接受 |
| T10 | **`vue-i18n`** | 中英双语，v1 至少覆盖界面文案 | 自研 i18n：省不了多少体积，丢失复数/插值等能力 |
| T11 | **CSS 变量 + `data-theme`** | 主题系统零运行时成本，便于导出只读页内联 | CSS-in-JS：运行时开销与导出复杂度都更高 |
| T12 | **Vitest + @vue/test-utils + Playwright** | 单测与组件测试复用 Vite 配置；E2E 覆盖"编辑→展示→导出"主链路 | —— |
| T13 | **vite-plugin-pwa** | 离线可用与安装能力，符合同类开源项目形态 | 手写 SW：生命周期与更新策略易出错 |
| T14 | **Tauri 2** | 复用同一套 Web 代码产出 exe/msi；安装包体积远小于 Electron；原生文件读写绕开 `file://` 与 CORS 限制 | Electron：体积 100MB+；仅 PWA：无法获得真正的本地文件系统能力 |
| T15 | **npm**（非 pnpm） | 与工作区既有项目（`TienChenInvoice`）保持一致，降低协作与 CI 心智成本 | pnpm：更快但引入第二套包管理器约定 |

### 5.1 依赖清单（预期）

```jsonc
{
  "dependencies": {
    "vue": "^3.5.0",
    "vue-router": "^4.5.0",
    "pinia": "^2.2.0",
    "vue-i18n": "^11.0.0",
    "vue-draggable-plus": "^0.6.0",
    "html-to-image": "^1.11.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "@vitejs/plugin-vue": "^5.2.0",
    "typescript": "^5.7.0",
    "vue-tsc": "^2.2.0",
    "vitest": "^2.1.0",
    "@vue/test-utils": "^2.4.0",
    "jsdom": "^25.0.0",
    "fake-indexeddb": "^6.0.0",
    "@playwright/test": "^1.49.0",
    "@vite-pwa/vite-plugin-pwa": "^0.21.0",
    "@tauri-apps/cli": "^2.1.0",
    "@tauri-apps/api": "^2.1.0"
  }
}
```

> **供应链纪律**：任何新增运行时依赖，必须在本文档 §5 表格中追加一行说明"为什么不用手写"。宁可手写 80 行，不引 80KB 依赖。

---

## 6. 数据模型（唯一真源）

### 6.1 类型定义

```ts
// src/types/project.ts

/** 语义化版本，便于未来迁移 */
export const SCHEMA_VERSION = 1

export interface Project {
  id: string                  // uuid v4
  schemaVersion: number       // 写入时的 SCHEMA_VERSION
  title: string               // 默认「未命名对比」
  createdAt: number           // epoch ms
  updatedAt: number
  tags: string[]              // v1 仅存储与筛选，不做标签管理 UI
  pinned: boolean
  sheet: Sheet                // v1 一个项目 = 一张对比页（v2 改为 sheets: Sheet[]）
  ui: ProjectUiState          // 视图态（展示模式、各侧配色覆盖等）
}

export interface ProjectUiState {
  mode: 'edit' | 'present'
  toolbarCollapsed: boolean
  sidebarCollapsed: boolean
  zoom: number                // 0.5 ~ 1.5，展示视图缩放
}

export interface Sheet {
  id: string
  /** v1 固定 2 侧；v2 允许 >2，渲染层按 sides.length 自适应列数 */
  sides: [Side, Side]
  rows: Row[]
  layout: LayoutConfig
}

export interface LayoutConfig {
  /** 两侧等宽 or 自定义比例；gutter 为中轴间距 */
  ratio: [number, number]
  gutter: number              // px
  density: 'compact' | 'normal' | 'comfy'   // 决定行距与内边距
  showAxis: boolean           // 是否显示中轴光带
  background: 'solid' | 'grid' | 'dots'
  maxWidth: number            // 内容最大宽度 px，默认 1440
}

export interface Side {
  id: string
  toolRef: ToolRef
  accent: string              // 主题色 hex，如 #A78BFA；默认左紫右青
  labelOverride?: string      // 覆盖工具名（例如「可灵 · 1.6 大师版」）
  modelVersion?: string       // 模型子名称/版本号，如 "v3.5" / "chirp-v4"
  note?: string               // 一句话备注，展示在工具名下方
}

export type ToolRef =
  | { kind: 'builtin'; toolId: string }
  | { kind: 'custom'; toolId: string }
  | { kind: 'inline'; name: string; iconAssetId?: string }  // 临时工具，不入库

export interface Row {
  id: string
  /** paired = 左右各一格；full = 单格跨两列 */
  kind: 'paired' | 'full'
  /** 行级标题（可选），显示在左右两格上方居中的胶囊标签 */
  label?: string
  cells: Record<SideId, Cell>   // sideId → Cell
  collapsed: boolean            // 编辑视图折叠该行
}

export interface Cell {
  modules: ModuleInstance[]
  hidden: boolean               // 整格在内侧隐藏（另一方仍显示）
  background?: 'none' | 'panel' | 'accent-soft'
}

export interface ModuleInstance {
  id: string
  type: ModuleTypeId
  title: string                 // 用户可改。默认 = 类型显示名，如「封面」→ 改成「价格」
  hidden: boolean               // 有内容但不想在展示视图显示
  data: unknown                 // 内容，由各模块类型的 schema 决定（见 §7.3）
  props: Record<string, unknown>
  locked?: boolean              // 锁定后不可删除（模板预置行可用）
}
```

```ts
// src/types/tool.ts
export interface Tool {
  id: string
  kind: 'builtin' | 'custom'
  name: string                  // Suno / 可灵 / Meshy
  vendor?: string               // Suno AI / 快手 / Tripo AI
  category: ToolCategory
  iconAssetId?: string          // 自定义图标（Asset 引用）
  iconUrl?: string              // 内置图标用内联 SVG data URI，见 §10.3
  color?: string                // 品牌色，用于自动配色建议
  homepage?: string
  aliases: string[]             // 搜索关键词，如 ['kling','可灵AI']
  builtinKey?: string           // 内置工具的唯一键，用于版本升级时合并
  createdAt: number
}

export type ToolCategory =
  | 'music' | 'image' | 'video' | 'audio-tts' | 'model-3d'
  | 'llm' | 'code' | 'product' | 'game' | 'other'
```

```ts
// src/types/asset.ts
export interface Asset {
  id: string                    // 内容哈希（sha-256 前 16 字节 hex），天然去重
  kind: 'image' | 'audio' | 'video' | 'text' | 'icon' | 'model3d'
  mime: string
  size: number
  name: string                  // 原始文件名
  createdAt: number
  blob: Blob                    // 本地文件与镜像缓存都有 blob
  origin?: {                    // 外链来源（若有）
    url: string
    mode: 'link' | 'mirror'     // link = 仅引用不缓存；mirror = 已镜像到本地
    fetchedAt?: number
    etag?: string
  }
  derived?: {                   // 派生数据，懒生成
    width?: number
    height?: number
    durationMs?: number         // 音视频时长
    thumbAssetId?: string       // 缩略图/封帧
    waveform?: number[]         // 降采样到 480 点的归一化振幅 0~1
  }
}
```

```ts
// src/types/settings.ts
export interface AppSettings {
  themeId: 'violet-dark'        // v1 仅一套，v2 扩展
  language: 'zh-CN' | 'en-US'
  restoreLastPosition: boolean  // 「保持位置」
  lastProjectId?: string
  lastMode?: 'edit' | 'present'
  defaultAccent: [string, string]        // 默认左紫右青
  accentPresets: Array<{ name: string; colors: [string, string] }>
  autosaveDebounceMs: number             // 默认 300，上限 2000
  mediaImportMode: 'ask' | 'mirror' | 'link'   // 外链默认处理策略
  maxMirrorSizeMB: number                // 默认 200，超过则拒绝镜像并提示
  sidebarWidth: number
  editorShowEmptyModules: boolean        // 编辑视图是否显示空模块（默认 true）
  exportScale: 1 | 2                     // 长图导出倍率，默认 2
  reducedMotion: 'auto' | 'always' | 'never'
}
```

### 6.2 存储布局（IndexedDB）

数据库名：`duet-db`，版本：`1`。

| Object Store | keyPath | 索引 | 用途 |
| --- | --- | --- | --- |
| `projects` | `id` | `updatedAt`, `pinned` | 对比项目（**不含任何 Blob**） |
| `assets` | `id` | `kind`, `createdAt` | 媒体二进制与派生数据 |
| `tools` | `id` | `kind`, `category`, `name` | 自定义工具（内置工具在代码里，不落库） |
| `settings` | `key` | —— | 单条记录 `key: 'app'` |
| `templates` | `id` | `category` | 用户自建模板（内置模板在代码里） |
| `meta` | `key` | —— | 迁移标记、最后备份时间、配额快照 |

**关键约束**：
1. `projects` 内**只允许存 assetId 字符串引用**，禁止内联 dataURL 或 Blob。违反即为架构性错误（会导致项目读写从毫秒级劣化到秒级）。
2. 所有结构变更必须走 `db/schema.ts` 的 `migrate(db, oldVersion, newVersion)`，**禁止**在业务代码里写"如果没有这个字段就补一个"的兼容分支。
3. 资产引用采用**引用计数惰性回收**：删除项目时不立即删资产；在设置页提供「清理未引用媒体」，扫描所有项目与工具后统一回收。

### 6.3 迁移策略

```ts
// src/db/schema.ts
const MIGRATIONS: Array<{ to: number; run: (db: IDBDatabase, tx: IDBTransaction) => void }> = [
  // { to: 2, run: (db, tx) => { /* v1→v2：sheet → sheets[] */ } },
]
```

规则：
- 每次 `SCHEMA_VERSION` +1，必须同时补一次迁移函数 + 一条测试用例（用 v(n-1) 的样例文件跑通）。
- **不允许删除迁移函数**，历史用户的库必须能一路升上来。
- 导入 `.duet` 文件时若 `schemaVersion > SCHEMA_VERSION`，**拒绝导入并提示"请升级应用"**，绝不尝试猜测。

### 6.4 工程文件格式（`.duet`）

```jsonc
{
  "$format": "duet-project",
  "schemaVersion": 1,
  "exportedAt": 1757900000000,
  "app": { "name": "对奏", "version": "0.4.0" },
  "project": { /* Project 原样，但不含 Blob */ },
  "tools": [ /* 该项目引用到的自定义工具 */ ],
  "assets": [
    {
      "id": "9f2c…",
      "kind": "image",
      "mime": "image/webp",
      "name": "suno-cover.webp",
      "size": 184320,
      "derived": { "width": 1024, "height": 1024, "thumbAssetId": "3a1b…" },
      "embedded": true,
      "data": "data:image/webp;base64,…"     // embedded=false 时省略
    }
  ],
  "integrity": { "missingAssets": [] }        // 导出时未能内嵌的资产 id 列表
}
```

导出对话框提供两个开关：
- **内嵌媒体**（默认开）：单文件可携带，体积大。
- **包含派生数据**（默认关）：带上缩略图与波形，打开即用但文件更大；关闭时导入后按需重建。

导入流程：解析 → 版本校验 → 资产去重（按 id，已存在则跳过）→ 工具合并（`builtinKey` 冲突则更新，自定义工具按 name 去重）→ 写入 → 打开。全过程有进度条，失败可回滚（先写临时记录，成功后再提交）。

---

## 7. 模块系统（本项目的心脏）

### 7.1 设计目标

用户所说的"添加文字模块后把标题改成'价格'、'简评'"——这不是特例，而是**核心机制**：**模块 = 类型（决定怎么渲染和编辑）+ 自定义标题（决定它在这张对比页里代表什么维度）**。因此：

- 同类型模块允许重复添加任意多次。
- 标题完全自由，不做任何枚举限制。
- 每个模块类型自带"呈现能力"（如封面图能换比例、音频能显示波形），但不自带"语义"。

### 7.2 架构：注册表 + 三个契约

**禁止出现巨型 `v-if/v-else` 分支**。所有模块类型通过注册表接入，新增一种模块 = 新增一个目录 + 一行注册，不改动任何既有代码。

```ts
// src/modules/registry.ts
export interface ModuleDefinition<TData = unknown, TProps = unknown> {
  type: ModuleTypeId
  /** 显示名与图标，供模块选择器与默认标题使用 */
  meta: { nameKey: string; icon: string; descriptionKey?: string; category: ModuleCategory }
  /** 数据校验与默认值 */
  schema: { create(): TData; validate(data: unknown): data is TData }
  /** 编辑器：编辑视图里的填写界面 */
  editor: DefineComponent
  /** 渲染器：展示视图里的只读呈现（含全部动效） */
  renderer: DefineComponent
  /** 内容判定：决定"是否算已填写"，用于空模块自动隐藏 */
  isEmpty(data: TData, props: TProps): boolean
  /** 可选：导出长图前的准备（等待媒体加载、字体就绪） */
  beforeExport?: (el: HTMLElement) => Promise<void>
  /** 可选：默认尺寸与呈现选项的元数据 */
  options?: Array<{ key: string; labelKey: string; type: 'select' | 'number' | 'boolean' | 'color'; values?: unknown[]; default: unknown }>
}

export const moduleRegistry = new Map<ModuleTypeId, ModuleDefinition>()
export function registerModule(def: ModuleDefinition): void

// src/modules/index.ts —— 唯一的注册入口
import './text'; import './cover'; import './lyrics'; /* … */
```

**模块目录标准结构**（以歌词模块为例）：

```
src/modules/lyrics/
├── index.ts          # 注册：type/meta/schema/editor/renderer/isEmpty
├── LyricsEditor.vue  # 编辑视图：文本域 + 导入 .txt/.lrc + 时间轴校验
├── LyricsRenderer.vue# 展示视图：竖滑 + 高亮当前行 + 自动滚动
├── schema.ts         # data 类型 + 创建 + 校验
├── lrc.ts            # LRC 解析（纯函数，可单测）
└── lyrics.spec.ts    # 单元测试
```

### 7.3 v1 模块清单

| type | 名称 | 内容数据（要点） | 编辑能力 | 展示动效 |
| --- | --- | --- | --- | --- |
| `text` | 文字 | `{ text: string; align; size }` | 多行输入 / 粘贴 | 逐段淡入 |
| `markdown` | Markdown | `{ md: string }` | 源码/预览双栏切换 | 打字机（可选） |
| `richText` | 富文本 | `{ html: string }` | contenteditable + 工具栏 | 同上 |
| `cover` | 封面图 | `{ assetId; fit; ratio }` | 上传/拖拽/外链/裁剪 | 缩放 + 遮罩渐变 |
| `image` | 图片 | `{ assetId; fit; ratio }` | 同上 | 同上 |
| `gallery` | 图片集 | `{ items: assetId[]; columns }` | 多选上传/排序 | 交错入场 |
| `audio` | 音频 | `{ assetId; showWaveform; showControls }` | 上传/外链 | 频谱条 + 播放脉冲 |
| `video` | 视频 | `{ assetId; posterAssetId?; autoplay; loop; muted }` | 上传/外链 | 悬停播放 / 循环 |
| `lyrics` | 歌词 | `{ text; lrc?: LrcLine[]; syncWithAudio: boolean }` | 手输 / 导入 .txt/.lrc | 竖滑 + 当前行高亮 + 自动滚动 |
| `progress` | 进度条 | `{ assetId?; durationMs; showTime; showWaveform }` | 绑定音频或手填时长 | 进度环/条 + 拖动预览 |
| `link` | 链接 | `{ url; label; desc; faviconAssetId? }` | 输入 URL 自动取标题 | 卡片浮起 |
| `keyValue` | 参数表 | `{ rows: {k,v}[]; columns: 1\|2 }` | 增删行 | 行交错入场 |
| `score` | 评分条 | `{ score: 0~10; max; label; showNumber }` | 滑杆 | 数字滚动 + 条填充 |
| `tagList` | 标签组 | `{ tags: string[]; tone }` | 回车添加 | 逐个弹出 |
| `divider` | 分割线 | `{ style; label? }` | 样式选择 | 宽度展开 |
| `placeholder` | 占位块 | `{ hint; height }` | 高度/提示 | 无 |
| `iframe` | 网页嵌入 | `{ url; height; sandbox: true }` | 输入 URL | 无（默认 sandbox） |
| `code` | 代码块 | `{ code; lang; theme }` | 输入/粘贴 | 行号渐显 |
| `diff` | 代码对比 | `{ left; right; lang; mode }` | 双栏输入 | 差异行高亮 |
| `model3d` | 3D 模型 | `{ assetId(.glb); autoRotate; bg }` | 上传 .glb/.gltf | 自动旋转（M5） |
| `timeline` | 时间线 | `{ events: {t,label,desc}[] }` | 增删事件 | 轴点依次点亮 |
| `note` | 备注 | `{ text; tone }` | 短文本 | 无 |
| `stars` | 星级 | `{ value: 0~5 }` | 点选 | 星星依次点亮 |

> **P0 必须实现**：`text` `cover` `image` `audio` `video` `lyrics` `progress` `link` `keyValue` `divider`（共 10 个，覆盖音乐/视频/图片/通用四类主场景）。
> **P1**：`markdown` `score` `tagList` `richText` `note` `stars` `placeholder`。
> **P2/P3**：其余。

### 7.4 空模块与隐藏规则（精确语义）

三个概念必须分清，`isEmpty` 判定由模块自己实现：

| 状态 | 判定 | 编辑视图 | 展示视图 |
| --- | --- | --- | --- |
| **空模块** | `def.isEmpty(data, props) === true` | 显示为虚线占位框 + 类型名 + "点击填写" | **不渲染**（DOM 中不存在） |
| **有内容 + `hidden: true`** | 用户手动关闭可见性 | 显示，标题旁有"眼睛划掉"图标 | **不渲染** |
| **有内容 + `hidden: false`** | 正常 | 显示 | 渲染 |

补充规则：
- 编辑视图可通过设置 `editorShowEmptyModules: false` 隐藏所有空模块（默认 **true**，因为编辑时看不到空模块会找不到位置）。
- `isEmpty` 的实现要求：文本类 `text.trim() === ''`；媒体类 `!assetId`；列表类 `items.length === 0`；评分类 **`score === null` 才算空，`score === 0` 不算空**（0 分是有效评分，这是最容易写错的地方，必须有测试）。
- **整行都空**时，编辑视图保留该行（否则无法继续填写），展示视图整行跳过。

### 7.5 行与对齐机制

- 对比页结构为 `rows: Row[]`，**一行的左右两格天然对齐**，由 CSS Grid 同一网格行承载，行高由较高者撑开，另一格 `align-self: stretch` 但内容 `align-self: start`。
- 不同高度的模块（如左侧视频、右侧文字）不会造成错位——这就是选择"行模型"而非"两个独立列表"的原因。
- 编辑视图每行左侧有拖拽手柄（`vue-draggable-plus`），支持上下排序；每格底部有"＋ 添加模块"按钮，弹出模块选择器（可按分类与搜索）。
- 支持把模块从一格拖到另一格（跨容器拖拽，`group: { name: 'cell', pull: true, put: true }`）。

---

## 8. 架构与目录结构

### 8.1 分层与依赖方向（单向，禁止反向）

```
            ┌──────────────────────────────────────────┐
            │  ui/  (Vue 组件、视图、样式)              │
            └───────────────┬──────────────────────────┘
                            │ 只能向下调用
            ┌───────────────▼──────────────────────────┐
            │  stores/  (Pinia：状态 + 命令分发)         │
            └───────────────┬──────────────────────────┘
                            │
        ┌───────────────────▼──────────────────────────┐
        │  services/  (业务编排：导入导出/同步播放/迁移) │
        └───────────────────┬──────────────────────────┘
                            │
   ┌────────────────────────▼─────────────────────────┐
   │  db/  (IndexedDB 仓储)   │  modules/ (模块注册表)  │
   └────────────────────────┬─────────────────────────┘
                            │
            ┌───────────────▼──────────────────────────┐
            │  lib/  (纯函数：无副作用、无依赖、可单测)  │
            └──────────────────────────────────────────┘
```

**硬性规则**：
1. `ui/` 禁止 `import` `db/`；一切持久化经 `stores/` → `services/`。
2. `lib/` 禁止 import 任何上层目录，禁止访问 `window`（除显式传入的 `document` 参数）。
3. 模块（`modules/*`）只能依赖 `lib/` 与 `types/`，**禁止直接访问 stores**；需要全局能力时通过 props 注入（如 `onSeek(t)`、`getAccent()`）。
4. 禁止跨模块 import 彼此的内部文件（`modules/a/inner.ts` 不可被 `modules/b` 引用）。

### 8.2 目录结构

```
Duet/                          ← 本地目录（仓库名 duet）
├── index.html
├── package.json
├── tsconfig.json  tsconfig.node.json
├── vite.config.ts
├── vitest.config.ts
├── playwright.config.ts
├── .eslintrc.cjs  .prettierrc
├── docs/
│   ├── 01-施工总案.md          ← 本文档
│   ├── 02-模块开发指南.md       ← 如何新增一种模块（M2 交付）
│   ├── 03-设计规范.md           ← 主题 token / 动效时长 / 间距表（M1 交付）
│   └── 04-验收清单.md           ← §16 的可勾选版本（M1 交付）
├── public/
│   ├── icons/tools/*.svg       ← 内置工具图标
│   └── manifest.webmanifest
├── src-tauri/                  ← Tauri 2（M5）
│   ├── tauri.conf.json
│   └── src/main.rs
└── src/
    ├── main.ts
    ├── App.vue
    ├── app.config.ts           ← 产品名/版本/扩展名，唯一来源
    ├── types/                  ← 领域类型（§6）
    │   ├── project.ts  tool.ts  asset.ts  settings.ts  module.ts  index.ts
    ├── lib/                    ← 纯函数
    │   ├── id.ts               # uuid / nanoid
    │   ├── hash.ts             # sha-256（WebCrypto，带降级）
    │   ├── time.ts             # ms → mm:ss.ms 格式化
    │   ├── lrc.ts              # LRC 解析与序列化
    │   ├── text.ts             # 截断/字数/高亮片段
    │   ├── color.ts            # hex/hsl 转换、对比度计算、配色建议
    │   ├── image.ts            # 缩略图生成、尺寸探测（OffscreenCanvas）
    │   ├── audio.ts            # 波形降采样（纯计算部分）
    │   ├── download.ts         # Blob 下载、文件选择
    │   ├── clone.ts            # 深拷贝（structuredClone 封装）
    │   ├── diff.ts             # 极简对象 diff → patch（撤销重做用）
    │   └── result.ts           # Result<T,E> 类型与助手
    ├── db/
    │   ├── core.ts             # openDB + 请求/事务 Promise 化
    │   ├── schema.ts           # DB 名/版本 + migrate()（唯一允许改结构处）
    │   ├── db.ts               # 连接单例
    │   ├── projectsRepo.ts  assetsRepo.ts  toolsRepo.ts  settingsRepo.ts  templatesRepo.ts
    │   └── quota.ts            # navigator.storage.persist/estimate 封装
    ├── services/
    │   ├── projectService.ts   # 新建/复制/删除/重命名/自动保存调度
    │   ├── assetService.ts     # 导入、哈希去重、派生数据生成、镜像外链、回收
    │   ├── mediaResolver.ts    # assetId/URL → 可渲染 src（blob URL 池 + 缓存 + 错误态）
    │   ├── toolService.ts      # 内置工具加载、自定义工具 CRUD、图标处理
    │   ├── exportService.ts    # .duet / 长图 PNG / 只读 HTML
    │   ├── importService.ts    # .duet 解析、校验、合并、回滚
    │   ├── templateService.ts  # 内置模板实例化（生成初始 rows/sides）
    │   ├── audioSyncService.ts # Web Audio 双轨同步播放（§12）
    │   └── migrationService.ts # schema 迁移 + 数据自检/修复
    ├── stores/
    │   ├── useProjectStore.ts  # 当前打开项目、标签页、脏标记
    │   ├── useProjectsStore.ts # 项目列表、排序、搜索
    │   ├── useToolsStore.ts    # 工具库
    │   ├── useSettingsStore.ts # 设置（含主题/语言）
    │   ├── useUiStore.ts       # 视图模式、弹窗、Toast
    │   ├── useHistoryStore.ts  # 撤销重做栈
    │   └── commands.ts         # **唯一的写入口**：dispatch(action) → 应用 patch → 入栈 → 派发持久化
    ├── modules/
    │   ├── registry.ts  index.ts  types.ts
    │   └── text/ cover/ image/ audio/ video/ lyrics/ progress/ link/ keyValue/ divider/ …
    ├── components/
    │   ├── layout/  AppShell.vue  Sidebar.vue  TabBar.vue  Toolbar.vue
    │   ├── compare/ CompareCanvas.vue  CompareRow.vue  CompareCell.vue
    │   │             SideHeader.vue   AxisGlow.vue   EmptyModuleSlot.vue
    │   ├── editor/  ModulePicker.vue  InspectorPanel.vue  RowToolbar.vue
    │   ├── present/ PresentOverlay.vue  PresentToolbar.vue
    │   └── common/  AppButton.vue  AppModal.vue  AppToast.vue  AppSelect.vue
    │                 AppColorPicker.vue  AppProgress.vue  AppEmpty.vue  AppIcon.vue
    ├── views/
    │   ├── CompareView.vue     # 对比界面（编辑/展示双模）
    │   ├── SettingsView.vue    # 设置界面
    │   └── settings/ ThemePanel.vue  LanguagePanel.vue  ToolsPanel.vue
    │                 CompareDefaultsPanel.vue  StoragePanel.vue  AboutPanel.vue
    ├── i18n/  index.ts  zh-CN.ts  en-US.ts
    ├── styles/
    │   ├── tokens.css          # ★ 主题 token 唯一来源（§11）
    │   ├── base.css  reset.css  utilities.css  animations.css  print.css
    └── router/ index.ts
```

### 8.3 状态管理：单一写入口（本项目最关键的架构决策）

**所有对项目数据的修改必须经过 `dispatch`**，禁止在组件里直接 `project.rows.push(...)`。

```ts
// src/stores/commands.ts（示意）
export type Command =
  | { t: 'row/add';      row: Row; at?: number }
  | { t: 'row/remove';   rowId: string }
  | { t: 'row/move';     rowId: string; to: number }
  | { t: 'row/relabel';  rowId: string; label: string }
  | { t: 'module/add';   rowId: string; sideId: string; type: ModuleTypeId; at?: number }
  | { t: 'module/remove';rowId: string; sideId: string; moduleId: string }
  | { t: 'module/move';  from: CellRef; to: CellRef; moduleId: string; toIndex: number }
  | { t: 'module/patch'; ref: ModuleRef; patch: Partial<ModuleInstance> }
  | { t: 'module/data';  ref: ModuleRef; patch: Record<string, unknown> }  // 深合并到 data
  | { t: 'side/patch';   sideId: string; patch: Partial<Side> }
  | { t: 'sheet/patch';  patch: Partial<Pick<Sheet, 'layout'>> }
  | { t: 'project/patch';patch: Partial<Pick<Project, 'title' | 'tags' | 'pinned'>> }

export function dispatch(cmd: Command, opts?: { transient?: boolean; coalesceKey?: string }): void
```

由此**一次性获得四个能力**：

| 能力 | 怎么来的 |
| --- | --- |
| 撤销/重做 | 每个 Command 天然可逆；`HistoryStore` 存反向 patch，栈深 100 |
| 自动保存 | 只有 dispatch 会触发 `updatedAt` 与防抖落盘，不会漏也不会重复 |
| 协同/同步（v2） | Command 即操作日志，未来上行到服务端就是现成的 CRDT 前置 |
| 可测试 | 纯函数式的命令应用 `applyCommand(project, cmd)` 可脱离 DOM 单测 |

**连续输入合并**：`coalesceKey`（如 `module:text:${moduleId}`）在 800ms 内的同类命令合并为一条历史记录，避免"打一行字撤销 40 次"。

### 8.4 媒体解析层（`mediaResolver`）

统一解决"assetId 或外链 URL → 能放进 `<img>/<audio>/<video>` 的 src"：

```ts
type MediaSource =
  | { kind: 'asset'; assetId: string }
  | { kind: 'url'; url: string; mirror?: boolean }

interface ResolvedMedia {
  src: string                 // blob: 或 https:
  status: 'ready' | 'loading' | 'error'
  error?: 'cors' | 'network' | 'unsupported' | 'too-large'
  revoke(): void
}

export function resolveMedia(src: MediaSource, opts?: { kind: 'image' | 'audio' | 'video' }): ResolvedMedia
export function useResolvedMedia(src: MaybeRef<MediaSource>, kind: MediaKind): Ref<ResolvedMedia>
```

实现要点：
1. **blob URL 池**：按 `assetId` 缓存，引用计数为 0 后延迟 10s 再 `revokeObjectURL`（避免快速切换视图时反复创建）。
2. **并发闸门**：同一时刻最多 4 个 blob 读取，防止打开大项目时 IO 拥塞。
3. **失败必可见**：`status === 'error'` 时渲染统一的 `MediaErrorPlaceholder`（带具体原因文案 + "重新加载"按钮），**绝不允许静默空白**。
4. **revoke 时机**：组件 `onUnmounted` 时必须调用 `revoke()`；在展示视图切回编辑视图时统一释放。

### 8.5 外链媒体策略（CORS 与防盗链）

这是同类 Web 应用最容易失败的功能，必须按阶梯降级设计：

```
① 用户粘贴外链
    ↓
② 尝试 <img crossorigin="anonymous"> / fetch(url) 探测可读性（3s 超时）
    ↓
③ 可读 → 按用户设置处理：
     mirror（默认）  → 完整下载为 Blob 存入 assets，之后完全本地化，导出也能内嵌
     link            → 仅保存 URL，展示时直接用（首屏快，但导出长图可能空白）
    ↓
④ 不可读（CORS / 防盗链 / 需 Referer）
     → 弹窗三选一：
       (a) 走本地代理缓存（仅 dev 与 Tauri 提供；见下）
       (b) 保留为外链引用，标注"⚠ 可能无法导出长图"
       (c) 取消，改为手动上传本地文件
```

- **开发环境代理**：Vite 中间件 `/__media_proxy?url=` 直连抓取（仅 dev）。
- **Tauri 桌面版**：暴露 `fetch_remote(url)` 原生命令，**完全绕开浏览器 CORS**——这是桌面版最实在的收益之一，必须在 M5 落地。
- **纯 Web 生产环境**：不提供代理（保持零后端承诺），只做 (b)/(c) 提示。
- 所有外链请求：超时 15s、最大体积受 `maxMirrorSizeMB` 限制、失败重试 1 次、错误分类写进 `ResolvedMedia.error`。
- **安全**：镜像的内容一律按原始字节存储，不做任何解析执行；`iframe` 模块强制 `sandbox="allow-scripts"` 且默认关闭，并在 UI 上明示风险。

### 8.6 导出长图（`html-to-image` 加固）

该库在网络字体、跨域图片、Safari 三种情况下会产出空白图，必须做以下加固：

```ts
async function exportLongImage(el: HTMLElement, opts: { scale: 1 | 2 }): Promise<Blob> {
  await document.fonts.ready                       // ① 字体就绪
  await waitForImages(el)                          // ② 所有 <img> complete && naturalWidth>0
  await waitForMediaElements(el)                   // ③ <audio>/<video> 的 poster 或首帧可用
  await inlineExternalImages(el)                   // ④ 外链图片转 dataURL（blob 已本地化的跳过）
  const bg = getComputedStyle(document.documentElement).getPropertyValue('--bg-base')
  const dataUrl = await toPng(el, {
    pixelRatio: opts.scale,
    backgroundColor: bg.trim(),
    cacheBust: true,
    skipFonts: false,
    filter: (node) => !node.classList?.contains('no-export'),   // 排除工具栏等
  })
  return dataUrlToBlob(dataUrl)
}
```

- 长图模式：临时把滚动容器设为 `height: auto; overflow: visible`，截图后还原（用 `document.body.dataset.exporting = '1'` 切换一份专用 CSS，隐藏工具栏、播放按钮等 `.no-export` 元素）。
- 校验：`pixelRatio` 与总高度相乘不得超过 16384px（浏览器 canvas 上限），超限时自动降 `pixelRatio` 并提示。
- **回归测试**：Playwright 中截图后断言 PNG 的**非透明像素占比 > 5%**，直接堵死"导出纯白图"这类事故。

### 8.7 只读 HTML 导出

生成单个 `.html` 文件：内联全套 CSS（`tokens.css` + 组件样式，构建时通过 `?inline` 导入）、内联一份极小的只读渲染运行时、媒体以 data URI 内嵌（小文件）或写入同名 `.assets/` 目录（大文件，选择"分离媒体"时）。

内容与展示视图**视觉一致但交互极简**：可播放音视频、可滚动歌词，其余全部静态。顶部带一行极小的来源标记（可关闭）。

---

## 9. 界面设计

### 9.1 应用外壳（AppShell）

```
┌──────────────────────────────────────────────────────────────────────────┐
│  [☰]  对奏            ⟵ 顶栏：Logo / 全局搜索 / 视图切换 / 导出 / 设置 ⚙ │  H=52
├───────────────┬──────────────────────────────────────────────────────────┤
│ 项目列表       │  标签页：[● Suno vs Lyria ✕][ 可灵 vs 即梦 ✕][ ＋ ]        │  H=40
│ ┌───────────┐ ├──────────────────────────────────────────────────────────┤
│ │🔍 搜索     │ │                                                          │
│ ├───────────┤ │                                                          │
│ │★ 置顶      │ │                                                          │
│ │ ▸ Suno…    │ │              对 比 画 布（编辑 / 展示）                   │
│ │ ▸ 可灵…    │ │                                                          │
│ │ ▸ Tripo…   │ │                                                          │
│ ├───────────┤ │                                                          │
│ │ 最近       │ │                                                          │
│ │ ▸ 价格对比  │ │                                                          │
│ └───────────┘ │                                                          │
│  ＋ 新建对比   │                                                          │
└───────────────┴──────────────────────────────────────────────────────────┘
   宽 260（可拖拽 200~400，持久化）
```

- 侧栏可折叠（`Ctrl/Cmd + B`），折叠后仅留 48px 图标条。
- 项目项右键菜单：重命名 / 复制 / 导出 / 置顶 / 删除（删除需二次确认，提示"可在 10 秒内撤销"，用软删除实现）。
- 标签页仅显示**已打开**的项目；关闭标签不删除项目。

### 9.2 对比界面（核心）

**工具头（SideHeader）**
```
┌───────────────────────────────┐   ┌───────────────────────────────┐
│ ◉  Suno AI          v3.5      │   │ ◉  Lyria (Google Music)  v2   │
│    [图标 40×40]  Suno          │   │    [图标 40×40]  Lyria        │
│    一句话备注（可选）           │   │    一句话备注（可选）           │
└───────────────────────────────┘   └───────────────────────────────┘
```
- 可点击图标直接替换；点击名称就地编辑（`labelOverride`）；点击版本号编辑 `modelVersion`。
- 头部底色为 `accent` 的 8% 淡色，顶部 2px 为 `accent` 实色条——这是"每侧主题色"最直观的落点。

**编辑视图单行（以歌词行为例）**
```
  ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐   ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
  ⠿ 歌词                  👁 ✕ │   │ 歌词                  👁 ✕ ⠿      ← 拖拽手柄 / 可见性 / 删除
  │                           │   │                           │
  │  [文本域，可拖高]          │   │  [文本域]                 │
  │  ⬆ 导入 .txt / .lrc       │   │  ⬆ 导入 .txt / .lrc       │
  └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘   └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
              ＋ 添加模块                           ＋ 添加模块
```
- 空模块：虚线框 + 类型图标 + "点击填写"。
- 模块标题可直接双击改写（如把"文字"改成"价格"）。
- 每格悬停显示 `＋ 添加模块`；行悬停显示行级操作（在上下插入行 / 删除行 / 折叠）。

**展示视图**
- 编辑态的一切控件全部消失；顶部出现悬浮工具栏（鼠标移到顶部 80px 区域才出现，3 秒无操作淡出）：`退出展示 (Esc)` `全屏` `缩放` `同步播放` `导出`。
- 画布背景比编辑态更暗（`--bg-void`），两侧内容卡片有轻微浮起阴影，中轴显示 1px 渐变光带。
- 所有入场动效仅播放一次（切换项目或重进展示视图时重播）。

**三态对比**

| 区域 | 编辑视图 | 展示视图 | 只读 HTML |
| --- | --- | --- | --- |
| 工具头 | 可编辑 | 静态 + 光效 | 静态 |
| 模块 | 全部可见（含空模块虚线框） | 仅非空且未隐藏 | 同展示视图 |
| 拖拽/增删 | 有 | 无 | 无 |
| 播放控制 | 有 | 有（含同步播放） | 有（不联动） |
| 导出 | 有 | 有 | 无 |

### 9.3 设置界面

竖向分组卡片，左侧锚点导航：

| 分组 | 内容 |
| --- | --- |
| **外观** | 主题（v1 仅「紫夜 Violet Dark」，其余置灰并标"即将推出"）、动效强度（跟随系统/始终/关闭）、画布密度、背景样式、导出倍率 |
| **语言** | 简体中文 / English（切换即时生效，无需刷新） |
| **行为** | 保持位置（开关）、自动保存间隔、编辑视图显示空模块、侧栏宽度重置 |
| **对比默认值** | 默认左右主题色 + 配色预设管理、默认布局比例、中轴光带开关、新建项目的默认模板 |
| **工具库** | 内置工具列表（可停用/排序）、自定义工具 CRUD（名称/厂商/分类/图标上传/品牌色/主页/别名）、导入/导出工具库 JSON |
| **数据与存储** | 已用配额（`navigator.storage.estimate()` 可视化进度条）、申请持久化存储、清理未引用媒体、导出全部项目、导入 .duet、清空所有数据（二次确认 + 输入 "DELETE"） |
| **关于** | 版本、开源协议、仓库地址、快捷键一览、第三方许可 |

### 9.4 空状态与引导

- 无项目时：侧栏下方显示"＋ 新建对比"，主区域显示 4 张模板卡片（音乐 / 图片 / 视频 / 空白），点击即创建。
- 有项目但全部行为空：画布中央显示悬浮引导条"从下方添加第一个模块 ↓"，3 秒后淡出。
- 展示视图全空时：显示"还没有可展示的内容，请先在编辑视图填写"并给出一键返回按钮（避免用户对着白屏发懵）。

---

## 10. 内置工具库与图标

### 10.1 内置工具清单（v1 首批，按分类）

| 分类 | 工具 |
| --- | --- |
| 音乐生成 | Suno、Udio、Lyria (Google Music)、Riffusion、海绵音乐、天工 SkyMusic |
| 图像生成 | Midjourney、Stable Diffusion、Flux、DALL·E、即梦、通义万相、Ideogram |
| 视频生成 | 可灵 Kling、即梦 Dreamina、Runway、Pika、Luma、Sora、Vidu、海螺 |
| 语音合成 | IndexTTS、GPT-SoVITS、CosyVoice、Fish Audio、ElevenLabs、MiniMax Speech |
| 3D 生成 | Tripo3D、Meshy、Rodin、Hyper3D、Luma Genie |
| 大模型 | DeepSeek、GLM、Qwen、Kimi、Claude、GPT、Gemini |
| 代码/建站 | Cursor、Claude Code、v0、Bolt、Lovable |
| 其他 | 自定义占位（"产品 A/B"、"方案一/二"） |

**数据来源**：`src/data/builtin-tools.ts`，纯静态数组，随版本更新；**不落库**（避免升级时与用户库冲突）。用户对内置工具的"停用/排序"以 `settings.builtinToolOverrides` 记录，不修改原数组。

### 10.2 图标方案

- 内置工具：**不使用第三方真实 Logo**（版权风险）。采用「品牌色 + 名称首字母/首字」的**程序化生成图标**（SVG，由 `name` + `color` 渲染），风格统一且零版权风险。
- 允许用户在设置里为自己的工具上传真实图标（本地 Asset），仅存在于用户本地数据中，不由项目分发。
- 兜底：`color` 缺失时，由工具名哈希到一套预设色板（`lib/color.ts` 提供确定性映射，保证同名工具永远同色）。

### 10.3 自定义工具创建流程

```
设置 → 工具库 → ＋ 新建工具
  ├─ 名称（必填）
  ├─ 厂商 / 分类 / 主页 URL
  ├─ 品牌色（取色器 + 预设色板）
  ├─ 图标：上传图片（自动方形裁剪 + 128px webp）或留空（自动生成首字图标）
  ├─ 别名（逗号分隔，用于搜索）
  └─ 保存 → 立即出现在所有对比页的工具选择器中
```

---

## 11. 主题系统（紫夜 Violet Dark）

### 11.1 Token 唯一来源

`src/styles/tokens.css` 是**唯一**允许出现颜色字面量的文件。组件中一律 `var(--xxx)`；禁止 `color: #a78bfa` 这类硬编码（ESLint 自定义规则 + Code Review 双保险）。

### 11.2 紫夜主题 Token 表

```css
:root[data-theme='violet-dark'] {
  /* —— 背景层级 —— */
  --bg-void:        #06040C;   /* 展示视图最底层画布 */
  --bg-base:        #0B0714;   /* 应用底色 */
  --bg-surface:     #150F26;   /* 卡片 / 侧栏 / 顶栏 */
  --bg-surface-2:   #1D1533;   /* 卡片内嵌区块 / 输入框 */
  --bg-elevated:    #251B41;   /* 弹窗 / 下拉 / Tooltip */
  --bg-overlay:     rgba(6, 4, 12, .72);   /* 遮罩 */

  /* —— 描边 —— */
  --border-subtle:  #241B3D;
  --border-default: #2F2450;
  --border-strong:  #6B57B5;   /* 表单/按钮描边，与 --bg-surface 对比 3.24:1，满足 WCAG 1.4.11 */

  /* —— 文字（括号内为与 --bg-surface #150F26 的实测对比度） —— */
  --text-primary:   #F3EFFB;   /* 16.44:1  AAA */
  --text-secondary: #C4B9E0;   /* 10.07:1  AAA */
  --text-muted:     #8C82AA;   /*  5.22:1  AA   */
  --text-disabled:  #7C7499;   /*  4.27:1  仅禁用态使用，不承载信息 */
  --text-inverse:   #150F26;

  /* —— 主色（品牌紫） —— */
  --accent-100: #EDE9FE;  --accent-300: #C4B5FD;  --accent-500: #A78BFA;  /* 6.84:1 */
  --accent-600: #8B5CF6;  --accent-700: #7C3AED;  --accent-900: #4C1D95;
  --accent-fg:  #FFFFFF;
  --accent-soft:   rgba(139, 92, 246, .14);   /* 选中态底色 */
  --accent-ring:   rgba(139, 92, 246, .40);   /* 焦点环 */

  /* —— 对比双方默认色（左紫右青，语义对立且色相区分明显） —— */
  --side-a: #A78BFA;  --side-a-soft: rgba(167, 139, 250, .12);
  --side-b: #22D3EE;  --side-b-soft: rgba( 34, 211, 238, .12);

  /* —— 语义色 —— */
  --success: #34D399;  --warning: #FBBF24;  --danger: #F87171;  --info: #60A5FA;
  --success-soft: rgba(52, 211, 153, .14);
  --warning-soft: rgba(251, 191, 36, .14);
  --danger-soft:  rgba(248, 113, 113, .14);

  /* —— 渐变与光效 —— */
  --grad-brand:  linear-gradient(135deg, #8B5CF6 0%, #6366F1 50%, #22D3EE 100%);
  --grad-axis:   linear-gradient(180deg, transparent, rgba(167,139,250,.55), transparent);
  --glow-accent: 0 0 24px rgba(139, 92, 246, .35);
  --glow-side-a: 0 0 20px rgba(167, 139, 250, .28);
  --glow-side-b: 0 0 20px rgba( 34, 211, 238, .28);

  /* —— 阴影（暗色下靠深浅而非黑色阴影） —— */
  --shadow-sm: 0 1px 2px rgba(0,0,0,.40);
  --shadow-md: 0 6px 20px rgba(0,0,0,.45);
  --shadow-lg: 0 16px 48px rgba(0,0,0,.55);
  --shadow-pop: 0 12px 40px rgba(0,0,0,.60), 0 0 0 1px var(--border-default);

  /* —— 圆角 —— */
  --radius-xs: 4px;  --radius-sm: 6px;  --radius-md: 10px;
  --radius-lg: 16px; --radius-xl: 24px; --radius-full: 999px;

  /* —— 间距（4 的倍数） —— */
  --sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px; --sp-4: 16px;
  --sp-5: 20px; --sp-6: 24px; --sp-8: 32px; --sp-10: 40px; --sp-12: 48px;

  /* —— 字号与行高 —— */
  --font-sans: 'Inter var', 'HarmonyOS Sans SC', 'PingFang SC',
               'Microsoft YaHei UI', system-ui, -apple-system, sans-serif;
  --font-mono: 'JetBrains Mono', 'Cascadia Code', Consolas, monospace;
  --fs-xs: 12px; --fs-sm: 13px; --fs-base: 14px; --fs-md: 15px;
  --fs-lg: 18px; --fs-xl: 22px; --fs-2xl: 28px; --fs-3xl: 36px;
  --lh-tight: 1.25; --lh-normal: 1.5; --lh-relaxed: 1.7;

  /* —— 动效（§12 使用） —— */
  --dur-instant: 80ms;  --dur-fast: 140ms; --dur-base: 220ms;
  --dur-slow: 380ms;    --dur-slower: 620ms;
  --ease-out:   cubic-bezier(.22, 1, .36, 1);
  --ease-in-out:cubic-bezier(.65, 0, .35, 1);
  --ease-spring:cubic-bezier(.34, 1.56, .64, 1);

  /* —— 层级 —— */
  --z-canvas: 1; --z-sticky: 100; --z-toolbar: 200;
  --z-modal: 500; --z-toast: 900; --z-present: 1000;
}
```

### 11.3 对比双方主题色机制

- 每个 `Side` 有 `accent`，渲染时写入该侧容器的内联 CSS 变量：
  ```html
  <section class="side" :style="{ '--accent': side.accent, '--accent-soft': hexToSoft(side.accent) }">
  ```
- 模块样式统一使用 `var(--accent)`（就近继承），于是一个模块组件在左右两侧自动呈现不同色，**无需为左右写两套样式**。
- 配色预设（`settings.accentPresets`）默认提供：紫/青、粉/蓝、橙/绿、红/灰、金/银。

### 11.4 未来主题扩展

`data-theme` 值驱动，新增主题 = 新增一个 `[data-theme='xxx']` token 块 + 在设置里加一项。**禁止**在组件内写主题判断分支。

---

## 12. 动效系统

### 12.1 原则

1. **动效服务于"对比"，不服务于炫技**：所有动效都必须强化"左右并置"这一信息结构（如两侧同时入场、中轴光带自上而下扫过）。
2. **时长纪律**：入场 ≤ 620ms，交互反馈 ≤ 220ms，悬停 ≤ 140ms。禁止超过 1s 的动效。
3. **可关闭**：`settings.reducedMotion` 与 `@media (prefers-reduced-motion: reduce)` 双重生效，关闭后所有动效降级为瞬时切换（保留状态变化，去掉位移与缩放）。
4. **只播一次**：入场动效由 `IntersectionObserver` 触发，元素进入视口播放一次，滚动回看不再播。

### 12.2 动效清单

| ID | 名称 | 位置 | 触发 | 时长/曲线 | 描述 |
| --- | --- | --- | --- | --- | --- |
| A1 | 双侧同步入场 | 对比画布 | 进入展示视图 | 420ms staggered 60ms / ease-out | 左右模块成对从下方 12px 淡入，同一行的左右同时开始 |
| A2 | 中轴光带 | 两列之间 | 展示视图常驻 | 6s 循环 / linear | 1px 竖向渐变光从上到下缓慢扫描，强化"分隔与对峙" |
| A3 | 工具头高光 | SideHeader 顶部条 | 入场 / 悬停 | 380ms / ease-out | accent 色条从中间向两侧展开 |
| A4 | 封面浮现 | cover/image 模块 | 入场 | 620ms / ease-out | `scale(1.04) → 1` + 不透明度 0→1 + 底部渐变遮罩浮现 |
| A5 | 播放脉冲 | audio 模块 & 进度条 | 播放中 | 1.6s 循环 / ease-in-out | accent 光晕呼吸（`box-shadow` 变化，避免重排） |
| A6 | 频谱律动 | audio 模块 | 播放中 | rAF 驱动 | 48 根竖条随 `AnalyserNode` 实时数据起伏，峰值回落有阻尼 |
| A7 | 歌词行高亮 | lyrics 模块 | 播放进度变化 | 260ms / ease-out | 当前行 `--text-primary` + 轻微左移 4px + 左侧 2px accent 竖条 |
| A8 | 进度环/条填充 | progress 模块 | 播放中 | rAF / linear | 进度条带渐变与前端光点；拖动时显示时间气泡 |
| A9 | 数字滚动 | score/keyValue 模块 | 入场 | 700ms / ease-out | 从 0 滚到目标值，末尾有 6% 回弹 |
| A10 | 行交错入场 | keyValue/timeline/gallery | 入场 | 40ms stagger / ease-out | 子项依次淡入上移 8px |
| A11 | 标签弹出 | tagList | 入场 | 220ms / ease-spring | 逐个 `scale(.88)` → 1 |
| A12 | 空模块呼吸 | 编辑视图空模块 | 悬停 | 220ms | 虚线框变 accent 色 + 提示文字浮出 |
| A13 | 视图切换 | 编辑 ↔ 展示 | 切换 | 320ms / ease-in-out | 编辑态控件淡出并 `translateY(-6px)`，展示态画布底色下沉到 `--bg-void` |
| A14 | 保存指示 | 顶栏 | dispatch 后防抖 | 140ms | 小圆点闪现 + "已保存"胶囊淡出（1.2s） |
| A15 | 同步播放启动 | 两侧播放器 | 点"同步播放" | 380ms | 两侧进度条同时从 0 走到起点，accent 光带闪一下 |

### 12.3 性能纪律

- 只允许动画 `transform` / `opacity` / `filter` / `box-shadow`；**禁止**动画 `width/height/top/left`（进度条用 `transform: scaleX()` 实现）。
- 频谱与进度用**单个 `requestAnimationFrame` 循环**集中驱动（`services/audioSyncService` 暴露一个 `useRafTick()`），禁止每个组件各自 `setInterval`。
- 页面不可见时（`document.hidden`）暂停 rAF 与频谱计算。
- 展示视图若模块数 > 60，自动关闭 A2/A5/A6 的高开销项并提示（性能预算见 §15）。

---

## 13. 音频双轨同步播放（A/B 对比试听）

### 13.1 目标

同一套提示词下两个工具生成的音乐，要能**从同一时刻起播、可同步定位、可 solo 单听**，靠人耳判断差异。

### 13.2 技术方案

- **唯一时钟源**：`AudioContext.currentTime`。两个 `<audio>` 元素经 `createMediaElementSource` 接入同一 `AudioContext` 的两个 `GainNode`。
- **起播**：`play()` 时记录 `t0 = ctx.currentTime`，对两侧分别计算 `offset`（用户设定的偏移量，用于补偿前奏差异），通过 `el.currentTime = offset` 对齐后再 `play()`。
- **漂移校正**：每 250ms 采样一次两侧 `currentTime` 差值 `Δ`；`|Δ| > 120ms` 时对落后方执行 `currentTime = 领先方 - offset` 硬校正；`320ms` 后进入稳定区，避免抖动。
- **主时钟选择**：默认以**时长较长**的一方为主时钟；若用户点了某一侧的"设为主轨"，则以其为准。
- **Solo / Mute**：Solo 时把另一侧 `GainNode.gain` 线性淡到 0（80ms），而非直接暂停——保证时钟不中断、切回来仍同步。
- **音量**：各自独立的对数音量滑块（0~1 → `gain` 曲线），默认 0.8。
- **频谱**：两侧各接一个 `AnalyserNode`（`fftSize: 256`），数据驱动 A6 动效。

### 13.3 与歌词/进度的联动

- 若某侧含 `lyrics` 模块且 `syncWithAudio: true`，则该侧歌词按 `audio.currentTime` 定位当前行并平滑滚动（`scrollIntoView({ block: 'center', behavior: 'smooth' })`）。
- 若歌词无 LRC 时间轴，则按"总行数均分总时长"做**估算对齐**，并在 UI 上标注"估算同步"，避免用户误以为是精确同步。
- `progress` 模块绑定 `assetId` 时自动跟随该侧播放进度；拖动进度条调用 `seek(t)`，两侧同时定位（保持 offset）。

### 13.4 浏览器策略与降级

- 自动播放受限：**必须由用户手势触发首次播放**（工具栏按钮），不得在进入展示视图时自动播放。
- 无 `AudioContext`（极老环境）或 `createMediaElementSource` 失败（跨域媒体未带 CORS 头）时，降级为"两个独立播放器 + 手动对齐"，并在 UI 明确提示原因。**降级路径必须有测试覆盖**。
- 跨域外链音频：`<audio crossorigin="anonymous">` 失败则无法接入分析器与增益控制，此时自动走降级路径（仍可播放，但不能同步/A-B）。

---

## 14. 快捷键、无障碍、国际化

### 14.1 快捷键

| 组合 | 作用 | 作用域 |
| --- | --- | --- |
| `Ctrl/Cmd + N` | 新建对比 | 全局 |
| `Ctrl/Cmd + O` | 打开/导入工程文件 | 全局 |
| `Ctrl/Cmd + S` | 立即保存（手动落盘 + 导出提示） | 全局 |
| `Ctrl/Cmd + E` | 切换编辑 / 展示视图 | 对比页 |
| `Ctrl/Cmd + B` | 折叠 / 展开侧栏 | 全局 |
| `Ctrl/Cmd + Z` / `Shift + Ctrl/Cmd + Z` | 撤销 / 重做 | 对比页 |
| `Ctrl/Cmd + D` | 复制当前行 | 对比页 |
| `Ctrl/Cmd + K` | 打开命令面板（跳项目 / 加模块 / 切主题） | 全局 |
| `Space` | 播放 / 暂停（音频模块聚焦时；展示视图全局可用） | 对比页 |
| `← / →` | 两侧各后退 / 前进 5 秒 | 展示视图 |
| `Shift + ← / →` | 微调 1 秒 | 展示视图 |
| `1` / `2` | Solo 左 / 右侧音轨 | 展示视图 |
| `0` | 取消 Solo（恢复双轨） | 展示视图 |
| `Esc` | 退出展示视图 / 关闭弹窗 | 全局 |
| `F` | 全屏 | 展示视图 |
| `?` | 快捷键一览 | 全局 |
| `Delete / Backspace` | 删除选中模块（需先选中） | 编辑视图 |
| `Ctrl/Cmd + ↑ / ↓` | 上移 / 下移当前行 | 编辑视图 |

**实现纪律**：所有快捷键经 `services/hotkeys.ts` 注册表统一处理，输入框聚焦时（`input, textarea, [contenteditable]`）默认屏蔽除 `Esc`/`Ctrl+S` 外的所有快捷键。

### 14.2 无障碍（AA 基线）

**实测对比度**（相对 `--bg-surface #150F26`，已在 M0 用脚本核验，见 §21.3）：

| Token | 值 | 对比度 | 判定 |
| --- | --- | --- | --- |
| `--text-primary` | `#F3EFFB` | 16.44:1 | AAA |
| `--text-secondary` | `#C4B9E0` | 10.07:1 | AAA |
| `--text-muted` | `#8C82AA` | 5.22:1 | AA ✓ |
| `--text-disabled` | `#7C7499` | 4.27:1 | 仅禁用态，不承载信息 |
| `--accent-500` | `#A78BFA` | 6.84:1 | AAA（可直接作正文强调色） |
| `--side-b` | `#22D3EE` | 10.30:1 | AAA |
| `--success` / `--warning` / `--danger` / `--info` | 见 §11.2 | 9.68 / 11.15 / 6.73 / 7.32 | 全部 ≥ 4.5:1 |

其他要求：
- `--border-strong`（`#6B57B5`，3.24:1）专用于**表单与按钮描边**——WCAG 1.4.11 要求交互控件的视觉边界达到 3:1；弱描边（`--border-subtle` 1.15:1、`--border-default` 1.32:1）只能用于纯装饰性分隔。
- 焦点可见：统一 `outline: 2px solid var(--accent-ring); outline-offset: 2px`，禁止 `outline: none`。
- 交互元素必须可 Tab 到达；拖拽排序提供键盘替代（选中后 `Ctrl+↑/↓`）。
- 图标按钮必须有 `aria-label`；播放/暂停用 `aria-pressed`；进度条用 `role="slider"` + `aria-valuenow/min/max`。
- 动效遵循 `prefers-reduced-motion`。
- 展示视图支持键盘完整操作（播放、Solo、进度），便于录屏时不出鼠标。
- **主题改动必须重跑 `scripts/check-contrast.mjs`**（见 §21.3），对比度不达标则 CI 失败。

### 14.3 国际化

- `vue-i18n` 组合式 API，`legacy: false`；文案 key 采用 `域.组件.元素`（如 `compare.sideHeader.modelVersion`）。
- 语言包全量加载（体积小），切换即时生效并写入 `settings.language`。
- **内置工具名不翻译**（Suno 就是 Suno），但分类名与所有 UI 文案翻译。
- 日期/数字用 `Intl` 格式化，禁止手写 `toLocaleString` 兜底分支。
- 首版必须做到：切换语言后**无任何硬编码中文残留**（用一条 lint 规则扫 `src/**/*.vue` 模板中的中文字面量，白名单仅限 `zh-CN.ts`）。

---

## 15. 性能预算

| 指标 | 目标 | 测量方式 |
| --- | --- | --- |
| 首屏可交互（冷启动，本地） | ≤ 1.5s | Lighthouse / Playwright trace |
| 首屏 JS（gzip） | ≤ 260KB | `vite build` 报告 |
| 首屏 CSS（gzip） | ≤ 40KB | 同上 |
| 切换视图（编辑↔展示，20 行） | ≤ 120ms | Performance mark |
| dispatch 到 UI 更新（单模块编辑） | ≤ 16ms | 开发者工具 Performance |
| 自动保存落盘（1MB 项目） | ≤ 60ms | `performance.now()` 包裹 |
| 打开含 50 个媒体的项目 | ≤ 800ms（骨架屏先出） | 手动测量 |
| 导入 200MB 视频 | UI 无阻塞，总耗时 ≈ 磁盘读速 | 手动测量 |
| 频谱动画帧率 | ≥ 55fps | DevTools FPS meter |
| 长图导出（1440×6000, scale 2） | ≤ 4s | 手动测量 |
| 空闲内存（含 3 个项目打开） | ≤ 500MB | Chrome Task Manager |

**达标手段**：
- 路由级代码分割：`SettingsView`、`PresentOverlay`、重型模块（`model3d`、`diff`）全部 `defineAsyncComponent`。
- `html-to-image` 与 Tauri API 动态 import，不进首屏包。
- 资产读取并发闸门（4）；波形数据在 `requestIdleCallback` 中生成。
- 长列表（>100 行）启用虚拟滚动（v2，v1 先做行数软上限提示：超过 200 行给出性能提醒）。
- Pinia 中 `assets` 只存元数据，Blob 通过 `mediaResolver` 按需取，**绝不把 Blob 放进响应式对象**（否则 Vue 会深度递归代理，直接卡死——这是必须写进开发规范的坑）。

---

## 16. 测试与验收

### 16.1 测试金字塔

| 层 | 工具 | 覆盖重点 | 目标覆盖率 |
| --- | --- | --- | --- |
| 单元 | Vitest | `lib/*`（lrc、color、time、hash、diff）、`applyCommand`、各模块 `isEmpty`/`schema.validate`、迁移函数 | `lib/` ≥ 90%，`services/` ≥ 70% |
| 组件 | Vitest + @vue/test-utils + fake-indexeddb | `CompareRow`、`CompareCell`、`ModulePicker`、`SettingsPanel` 各面板 | 关键交互路径全覆盖 |
| E2E | Playwright | 主旅程 5 条（见下） | 100% 通过 |
| 视觉 | Playwright 截图对比 | 展示视图在 4 种尺寸下的稳定截图 | 允许 0.2% 像素差 |

### 16.2 E2E 必测主旅程

1. **音乐对比全流程**：新建（音乐模板）→ 改两侧工具与版本 → 上传封面 → 导入 .lrc → 上传 mp3 → 切展示视图 → 同步播放 5s → 断言两侧 `currentTime` 差值 < 150ms → 导出长图 → 断言 PNG 非空白。
2. **持久化与位置恢复**：编辑 5 处 → 刷新页面 → 断言全部内容与视图模式恢复 → 关闭浏览器上下文重开 → 断言数据仍在。
3. **空模块规则**：新增 3 个空模块 → 展示视图断言 DOM 中不存在 → 填入其中 1 个 → 断言只有它出现 → 手动隐藏 1 个有内容的 → 断言不出现但在编辑视图仍可见。
4. **自定义模块反复添加**：同一格添加 3 个 `text` 模块，标题分别改为"价格""简评""结论" → 断言三者独立、互不影响、可单独删除。
5. **导入导出往返**：项目 A 导出 `.duet` → 清空数据库 → 导入 → 断言 `deepEqual(原 project, 新 project)`（含媒体尺寸、时长、波形长度）。

### 16.3 验收清单（Definition of Done）

一个功能算"完成"，必须同时满足：

- [ ] 功能符合 §4 该条目的描述
- [ ] 有单元/组件测试，`npm run test` 全绿
- [ ] 相关 E2E 用例通过
- [ ] 无 TypeScript 错误（`vue-tsc --noEmit` 通过）、无 ESLint 警告
- [ ] 硬编码颜色/文案检查通过（§11.1、§14.3 的 lint 规则）
- [ ] 键盘可达 + 焦点可见
- [ ] 在 `prefers-reduced-motion: reduce` 下无异常
- [ ] 在 Chrome / Edge / Firefox 最新版实测通过（Safari 尽力，问题记入 §20 风险）
- [ ] 文档已更新（新增模块类型必须同步 `docs/02-模块开发指南.md`）

---

## 17. 里程碑与任务拆解

> 每个里程碑结束时必须可运行、可演示、测试全绿。**不允许"半成品合并"**。

### M0 · 脚手架（0.5 天）

| # | 任务 | 产出 |
| --- | --- | --- |
| M0-1 | `npm create vite@latest` + Vue + TS 模板，配置 `strict`、路径别名 `@/`、ESLint/Prettier | 可运行空壳 |
| M0-2 | 建立 §8.2 完整目录骨架（含空 `index.ts` 与 TODO 注释） | 结构落地 |
| M0-3 | 配置 Vitest（jsdom + fake-indexeddb）、Playwright | `npm run test` / `test:e2e` 可跑 |
| M0-4 | `styles/tokens.css` 全量 token + `AppShell` 三栏布局 | 一眼看到紫夜主题 |
| M0-5 | ✅ **已完成**：`scripts/check-contrast.mjs` 主题对比度核验脚本（已实测通过，接入 `npm run check`） | AA 基线可自动守住 |
| M0-6 | Git 初始化、`.gitignore`、`README.md`、`LICENSE`(MIT) | 仓库可用 |

### M1 · 数据层 + 设置 + 主题（2 天）

| # | 任务 | 产出 |
| --- | --- | --- |
| M1-1 | `db/core.ts` + `schema.ts` + `db.ts`（Promise 化、迁移框架、错误处理） | DB 可读写 |
| M1-2 | 五个 repo + `projectsRepo` 全文测试 | 单测全绿 |
| M1-3 | `settingsRepo` + `useSettingsStore` + `SettingsView` 七分组（外观/语言/行为/默认值/工具/存储/关于） | 设置可改可存 |
| M1-4 | `vue-i18n` 接入 + zh-CN/en-US 全量 UI 文案 | 语言可切换 |
| M1-5 | `themeService`：`data-theme` 注入、`prefers-reduced-motion` 处理 | 主题生效 |
| M1-6 | 项目 CRUD + 侧栏项目列表 + 标签页 + 搜索排序 | 能建/开/删项目 |
| M1-7 | `lib/id.ts`/`hash.ts`/`time.ts`/`color.ts` + 单测 | 纯函数层就绪 |

### M2 · 编辑器核心（3 天）

| # | 任务 | 产出 |
| --- | --- | --- |
| M2-1 | `commands.ts` + `applyCommand` 纯函数 + `useHistoryStore`（撤销重做、合并） | 命令层 + 单测 |
| M2-2 | `moduleRegistry` + `ModulePicker` + 模块目录模板（含 `docs/02-模块开发指南.md`） | 模块架构 |
| M2-3 | **10 个 P0 模块**的 editor 与最小 renderer（`text` `cover` `image` `audio` `video` `lyrics` `progress` `link` `keyValue` `divider`） | 能填能看 |
| M2-4 | `CompareCanvas` / `CompareRow` / `CompareCell` / `SideHeader` + 空模块规则（§7.4） | 编辑视图可用 |
| M2-5 | `assetService`（导入、哈希去重、缩略图、时长、波形）+ `mediaResolver`（池化、并发闸门、错误态） | 媒体全链路 |
| M2-6 | 外链媒体阶梯降级（§8.5）+ 镜像缓存 + 错误提示 UI | 外链可用 |
| M2-7 | 拖拽排序（行 + 模块 + 跨格）+ 行/模块增删改 + 标题改写 | 编辑体验成型 |
| M2-8 | 自动保存（防抖 + `beforeunload` flush + 保存指示） | 数据不丢 |
| M2-9 | 每侧主题色 + 配色预设 + `InspectorPanel` | 对比双方可配色 |
| M2-10 | `toolService` + 内置工具库（§10.1）+ 自定义工具 CRUD + 程序化图标 | 生产源可管理 |

### M3 · 展示视图 + 导入导出（2.5 天）

| # | 任务 | 产出 |
| --- | --- | --- |
| M3-1 | `PresentOverlay`：只读隔离、悬浮工具栏、全屏、缩放、`Esc` 退出 | 展示视图可用 |
| M3-2 | 展示态规则：空/隐藏模块不渲染、行跳过、全空引导 | 符合 §7.4 |
| M3-3 | `.duet` 导出/导入（内嵌开关、去重合并、进度、回滚、版本校验） | 工程文件可用 |
| M3-4 | 长图 PNG 导出（§8.6 加固 + `.no-export` + 尺寸校验） | 长图可用 |
| M3-5 | 只读 HTML 导出（内联 CSS + 精简运行时 + 媒体内嵌/分离） | 分享可用 |
| M3-6 | 内置模板库（音乐/图片/视频/通用/空白） | 一键起步 |
| M3-7 | 快捷键体系（§14.1）+ 命令面板（`Ctrl+K`） | 效率可用 |

### M4 · 动效 + 音频同步（2 天）

| # | 任务 | 产出 |
| --- | --- | --- |
| M4-1 | `animations.css` + `useInView` + A1/A2/A3/A4/A9/A10/A11/A13/A14 | 视觉成型 |
| M4-2 | 统一 rAF 调度器 + A5/A6/A7/A8 播放类动效 | 播放动效 |
| M4-3 | `audioSyncService`：AudioContext 时钟、offset、漂移校正、Solo/Mute、音量 | 同步播放核心 |
| M4-4 | 频谱数据接入 + 歌词/进度联动（含估算同步标注） | 视听联动 |
| M4-5 | 降级路径（无 AudioContext / 跨域媒体）+ 明确提示 | 健壮性 |
| M4-6 | 动效开关与 `prefers-reduced-motion` 全量适配 | 无障碍达标 |
| M4-7 | 性能优化：代码分割、并发闸门、帧率核查（对照 §15） | 达预算 |

### M5 · 扩展与分发（2 天）

| # | 任务 | 产出 |
| --- | --- | --- |
| M5-1 | P1/P2 模块（`markdown` `score` `tagList` `richText` `note` `stars` `placeholder`） | 模块齐备 |
| M5-2 | P3 模块（`model3d` `diff` `timeline` `iframe` `gallery`） | 能力扩展 |
| M5-3 | PWA（离线可用、安装、更新提示、`storage.persist()`） | 离线可用 |
| M5-4 | Tauri 2 打包（exe/msi、原生文件读写、`fetch_remote` 绕 CORS、单实例、文件关联 `.duet`） | 桌面版 |
| M5-5 | 存储面板（配额可视化、清理未引用媒体、全量备份/恢复） | 数据可控 |
| M5-6 | 无障碍与 i18n 复查、README 双语、贡献指南、`docs/02/03/04` 补全 | 可开源 |
| M5-7 | 发布 v0.1.0（GitHub Release + 演示项目文件） | 交付 |

**总计约 12 个工作日**（不含评审与返工，按单人全职估算）。

---

## 18. 风险清单与对策

| # | 风险 | 影响 | 概率 | 对策 |
| --- | --- | --- | --- | --- |
| K1 | 用户导入大量/超大视频导致 IndexedDB 配额耗尽 | 数据写入失败、项目损坏 | 高 | 导入前 `estimate()` 预检；单文件默认上限 500MB 可调；超限时提示改用外链引用；写入失败必须事务回滚并明确报错；设置页常驻配额条 |
| K2 | 浏览器清空站点数据导致用户资产丢失 | 用户信任受损 | 中 | 启动时请求 `navigator.storage.persist()`；每次导出 `.duet` 后记录 `meta.lastBackupAt`；超过 7 天未备份时在顶栏显示提醒 |
| K3 | 外链媒体 CORS/防盗链导致导出长图空白 | 导出功能失效 | 高 | §8.5 三级降级 + §8.6 导出前外链转 dataURL + 导出后非空像素校验 + 导出前预检并列出"可能失败的资源"清单让用户先镜像 |
| K4 | `html-to-image` 在特定环境产出空白图（Safari/字体/跨域已知问题） | 核心导出不可用 | 中 | §8.6 四步加固；Playwright 像素校验纳入 CI；导出失败时回退方案：分屏截取（逐屏滚动截图后拼接，纯 Canvas 实现，不依赖第三方库） |
| K5 | 双轨同步音画漂移被用户察觉 | 对比结论失真 | 中 | 250ms 采样 + 120ms 阈值硬校正；UI 显示实时漂移值（高级选项）；提供"手动对齐偏移量"旋钮 |
| K6 | 大项目渲染卡顿（Blob 进入 Vue 响应式代理） | 页面卡死 | 中 | 写进开发规范 + Code Review 检查项；`assets` 元数据用 `shallowRef`/`markRaw`；ESLint 规则禁止在 `ref()` 中直接放 Blob |
| K7 | 移动端/Safari 兼容性不足 | 部分用户无法使用 | 中 | 明确"编辑需桌面 ≥1024px"（§2.5）；展示视图做响应式（单列堆叠）；Safari 问题单列清单，M4 集中处理 |
| K8 | 「行对齐」在极端内容（左超长文本 / 右小图）下视觉失衡 | 观感差 | 中 | 提供每格"垂直对齐"选项（顶/中/底）与"行级等高 vs 各自高度"开关；`layout.density` 调节 |
| K9 | 需求蔓延（N 方对比、多页、自由画布提前插队） | 里程碑失控 | 高 | §2.5 非目标写死；新需求一律进 §4.1 表并标注里程碑，**不允许插入当前里程碑** |
| K10 | Tauri 打包与 PWA 的路径/协议差异引发 bug | 分发版不稳定 | 中 | 抽 `platformAdapter`（`openFile/saveFile/fetchRemote/exists`）统一两套实现；E2E 在 Web 版跑，桌面版由 M5 验收清单人工过一遍 |
| K11 | 内置工具品牌 Logo 版权风险 | 法律风险 | 低 | §10.2 程序化首字图标，不内置任何第三方 Logo |

---

## 19. 开发规范

### 19.1 代码规范

- TypeScript `strict: true` + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`。
- 组件一律 `<script setup lang="ts">`，props 用 `defineProps<Props>()` 泛型写法，禁止运行时 `props: {}` 对象写法。
- 组件文件命名 PascalCase；组合式函数 `useXxx.ts`；纯函数文件 camelCase。
- 单个 `.vue` 文件 ≤ 300 行，超过则拆子组件或抽 composable。
- **禁止 any**（必要时 `unknown` + 类型守卫）。
- `console.log` 禁止进主干（保留 `lib/logger.ts` 的统一日志，生产环境静默）。

### 19.2 Git 规范

- 分支：`main`（可发布）/ `feat/xxx` / `fix/xxx`。
- 提交：Conventional Commits（`feat(modules): 新增 timeline 模块`）。
- 每个里程碑打 tag（`v0.1.0-m1`）。
- PR 必须：测试通过、`vue-tsc` 无错、文档已更新。

### 19.3 文档规范

| 文档 | 何时更新 |
| --- | --- |
| `docs/01-施工总案.md`（本文） | 任何范围/架构/数据模型变更，**先改文档再写代码** |
| `docs/02-模块开发指南.md` | 新增模块类型时 |
| `docs/03-设计规范.md` | token/动效/间距变更时 |
| `docs/04-验收清单.md` | 每次里程碑验收时勾选 |
| `CHANGELOG.md` | 每次发布 |

---

## 20. 决策记录（已全部拍板）

| # | 议题 | 决策 | 状态 |
| --- | --- | --- | --- |
| Q1 | **最终名称** | **对奏 / Duet**（本地目录 `Duet`，仓库与代码标识 `duet`） | ✅ 已定稿 |
| Q2 | 音轨手动偏移量旋钮（补偿前奏差异） | 需要，置于展示视图"高级选项"内 | ✅ 采纳（M4 实现） |
| Q3 | 展示视图自动轮播/演示模式 | v1 不做 | ✅ 采纳 |
| Q4 | 只读 HTML 导出的媒体内嵌策略 | 默认内嵌（< 20MB）；超过则分离到同名目录 | ✅ 采纳（M3 实现） |
| Q5 | 多语言歌词/字幕对齐 | 不做专门功能，用两个 `lyrics` 模块即可满足 | ✅ 采纳 |
| Q6 | 开源协议 | **AGPL-3.0**（原建议 MIT，用户指定更强 copyleft） | ✅ 已定稿 |
| Q7 | 项目分组/文件夹 | v1 用置顶 + 搜索 + 标签字段；分组留 v2 | ✅ 采纳 |
| Q8 | 内置工具库首批清单 | 按 §10.1 执行（用户已确认"内容尚可"） | ✅ 已确认 |

**协议变更说明**：AGPL-3.0 相比 MIT 的关键差异是**网络服务条款**——若有人把对奏改造成在线服务对外提供，必须公开其修改后的源码。对本地优先工具而言这不会影响正常使用，只在"被包装成商业云服务"时生效。`package.json` 中的 `license` 字段与 `LICENSE` 文件须保持一致（当前均为 AGPL-3.0-or-later）。

---

## 21. 附录

### 21.1 参考与致谢

- 应用形态参考：开源项目「魔方简介」（单页、离线、零账号）
- 信息组织参考：Obsidian（左侧列表 + 标签页 + 可携带工程文件）
- 内容结构参考：LMArena / Artificial Analysis（左右并置 + 分维度评分）
- 技术参考：
  - [html-to-image 在 Safari 下导出空白图的已知问题](https://github.com/bubkoo/html-to-image/issues/488)
  - [html-to-image 字体崩溃与 canvas 污染问题的修复案例](https://github.com/marimo-team/marimo/pull/8754)
  - [vue-draggable-plus 与 vuedraggable 的对比](https://npm-compare.com/ja-JP/vue-draggable-plus,vuedraggable/)
  - [Tauri 2 + Vue 3 打包实践](https://www.cnblogs.com/sjruxe/p/18664876)
  - [html2canvas / html-to-image / dom-to-image-more 对比](http://npm-compare.com/dom-to-image-more,html-to-image,html2canvas)

### 21.2 文档变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-09-15 | 首版：完成产品定位、数据模型、模块系统、紫夜主题、动效规范、音频同步方案、里程碑拆解、风险清单 |
| v1.1 | 2026-09-15 | 定名「对奏 / Duet」；协议定为 AGPL-3.0；§20 八项决策全部拍板；补充 M0 实施记录与实测数据（§21.4） |
| v1.2 | 2026-09-15 | M1 完成：命令层/校验/项目服务/资产服务/工具库/Store/七组设置面板/主界面；补充 §21.5 实施记录 |

### 21.3 对比度核验脚本（已落地并通过）

**已实现**：`scripts/check-contrast.mjs`（本仓库现存文件，非伪代码）。M0 需做的只是把它接入 `package.json` 的 `check` 脚本与 CI。

```jsonc
// package.json
"scripts": {
  "check:contrast": "node scripts/check-contrast.mjs",
  "check": "vue-tsc --noEmit && npm run check:contrast && npm run test"
}
```

脚本断言规则：
- **正文与语义色** ≥ 4.5:1（WCAG AA），覆盖 `--bg-surface` 与 `--bg-surface-2` 两种底色。
- **非文本元素**（按钮/表单描边、焦点环）≥ 3:1（WCAG 1.4.11）。
- 任一项不达标即退出码 1。

**首次运行实测输出**（已通过）：

```
=== 正文与语义色（AA 4.5:1） ===
PASS  text-primary      16.44:1      PASS  accent-500         6.84:1
PASS  text-secondary    10.07:1      PASS  side-b            10.30:1
PASS  text-muted         5.22:1      PASS  success            9.68:1
PASS  text-primary/2    15.37:1      PASS  warning           11.15:1
PASS  text-muted/2       4.88:1      PASS  danger             6.73:1
=== 非文本元素（AA 3:1） ===
PASS  border-strong      3.24:1      PASS  focus-ring         6.84:1

✅ 全部通过：紫夜主题满足 WCAG AA。
```

> **实战记录**：首版 token 中 `--border-strong` 取 `#453472`（1.76:1）与 `#57459A`（2.41:1），均不满足 1.4.11，已由本脚本抓出并修正为 `#6B57B5`（3.24:1）。这说明**主题颜色不能靠肉眼定稿**——脚本必须在 M0 就位。

### 21.4 M0 实施记录（已完成，2026-09-15）

**环境**：Node v24.19.0 / npm 11.17.0 / TypeScript 6.0.3 / Vite 8.3.0（Rolldown）/ Vue 3.5 / Playwright Chromium。

**M0 任务闭环**：

| 任务 | 状态 | 产出 |
| --- | --- | --- |
| M0-1 脚手架 | ✅ | Vite + Vue3 + TS(strict) + ESLint(flat) + Prettier + 路径别名 |
| M0-2 目录骨架 | ✅ | `types/ lib/ db/ services/ stores/ modules/ components/ views/ i18n/ styles/` 全部落地 |
| M0-3 测试链路 | ✅ | Vitest(jsdom+fake-indexeddb) + Playwright 均跑通 |
| M0-4 token + AppShell | ✅ | `tokens.css` 全量 token + 顶栏/侧栏/主区三区外壳 + 中英双语 |
| M0-5 对比度校验 | ✅ | `scripts/check-contrast.mjs`，14 项全部 PASS |
| M0-6 仓库就绪 | ✅ | `.gitignore` / `.gitattributes` / `.editorconfig` / `.env.example` / LICENSE(AGPL-3.0) / README |

**实测数据**（可作为后续回归基线）：

| 指标 | 实测 | 预算（§15） | 结论 |
| --- | --- | --- | --- |
| 单元测试 | 116 passed / 8 files | 全绿 | ✅ |
| E2E（chromium） | 4 passed | 全绿 | ✅ |
| 首次加载 JS（gzip） | ≈ 71 KB | ≤ 260 KB | ✅ |
| 首次加载 CSS（gzip） | ≈ 4.1 KB | ≤ 40 KB | ✅ |
| 生产构建耗时 | 473 ms | —— | ✅ |
| 构建模块数 | 128 modules | —— | ✅ |

**M0 期间实际发现并修复的问题**（都是真实缺陷，非环境噪音）：

| # | 问题 | 根因 | 修复 |
| --- | --- | --- | --- |
| 1 | `--border-strong` 不满足 WCAG 1.4.11 | 靠肉眼定色（1.76:1） | 改为 `#6B57B5`（3.24:1），并由脚本守住 |
| 2 | repo 层全部 import 失败（TS2307） | 文件位于 `src/db/` 内，却写成 `'../db'`（多退一级） | 全部改为 `'./db'` |
| 3 | `mergeSettings` 守卫全部失效 | `Object.assign(base, stored)` 把**回退源**本身改掉了，守卫比较的是同一份被污染的值 | 保留未污染的 `defaults`，新增类型与取值守卫 |
| 4 | `deepClone` 把 `Blob` 变成 `{}` | jsdom 的 `structuredClone(Blob)` **静默返回 `{}`**（不抛错，try/catch 兜不住） | 弃用 `structuredClone`，改为显式语义的递归克隆（宿主对象按引用保留） |
| 5 | `manualChunks` 构建报错 | Vite 8 底层 Rolldown 只接受函数形式 | 改为函数形式 |
| 6 | Playwright webServer 健康检查超时 | Vite 8 只监听 IPv6 `::1`，配置里写的是 `127.0.0.1` | baseURL 改用 `localhost`，并写进注释防复发 |

> 其中 #3 与 #4 是**会在用户侧造成数据损坏**的类型（脏设置崩溃、媒体静默丢失），被单测拦截在 M0 阶段，成本极低。

### 21.5 M1 实施记录（已完成）

**M1 任务闭环**：

| 任务 | 状态 | 产出 |
| --- | --- | --- |
| M1-1 命令层 | ✅ | `types/commands.ts`（15 种命令）+ `services/commands.ts`（`applyCommand` 纯函数） |
| M1-2 数据校验 | ✅ | `types/validate.ts`：项目/sheet/行/格/模块五级校验 + `collectAssetIds` |
| M1-3 项目服务 | ✅ | 工厂、复制（重建全部 id）、自动保存调度（防抖 + 页面退出 flush + 落盘前自检）、工程文件序列化/解析 |
| M1-4 资产服务 | ✅ | 内容哈希去重、尺寸/时长探测、外链阶梯降级、未引用资源回收 |
| M1-5 工具库 | ✅ | 47 个内置工具（10 分类，程序化图标）、自定义工具 CRUD、搜索、停用偏好 |
| M1-6 Store 层 | ✅ | `useProjectStore`（唯一写入口 + 撤销重做 + 自动保存）、`useProjectsStore`、`useToolsStore`、`useHistoryStore`、`useSidebarStore` |
| M1-7 设置面板 | ✅ | 七个分组全部实现（外观/语言/行为/对比默认值/工具库/数据与存储/关于） |
| M1-8 主界面 | ✅ | 侧栏真实项目列表（置顶/搜索/右键菜单/重命名/删除+撤销）、标签页、对比画布、工具头内联编辑 |
| M1-9 测试 | ✅ | 新增 42 条单测（命令层 21 + 项目服务 21），E2E 新增 11 条主旅程 |

**实测基线（增量）**：

| 指标 | M0 | M1 | 预算 |
| --- | --- | --- | --- |
| 单元测试 | 116 | **158** | 全绿 |
| E2E | 4 | **15** | 全绿 |
| 首屏 JS (gzip) | ≈71 KB | **≈71 KB** | ≤ 260 KB |
| 首屏 CSS (gzip) | ≈4.1 KB | **≈4.1 KB** | ≤ 40 KB |

**M1 期间发现并修复的问题**：

| # | 问题 | 根因 | 修复 |
| --- | --- | --- | --- |
| 1 | `mergeSettings` 守卫全部失效（M0 遗留的同类问题） | `Object.assign(base, stored)` 把回退源自身改掉 | 保留未污染的 `defaults`（M0 已修，M1 补了 `mediaImportMode`/`reducedMotion`/布尔字段守卫） |
| 2 | 自动保存句柄无法感知设置变更 | 防抖间隔在创建时被固化 | `AutosaveOptions.debounceMs` 支持取值函数，改设置立即生效 |
| 3 | 清空数据后路由仍指向已删除项目 | 只清了数据、没回收地址 | `StoragePanel` 清空后 `router.replace({ name: 'compare' })` |
| 4 | E2E 用例"靠运气通过" | 侧栏与画廊渲染同名模板卡，选择器未限定作用域 | 全部限定在 `<aside>` 内；这类**偶发通过**比失败更危险，已修正 |

### 21.6 M2 实施记录（已完成）

**已完成**：

| 任务 | 状态 | 产出 |
| --- | --- | --- |
| M2-1 模块契约与注册表 | ✅ | `modules/types.ts`（ModuleDefinition 三契约）+ `registry.ts` + `meta.ts` 一致性单测 |
| M2-2 十一个模块 | ✅ | text / cover / image / gallery / audio / video / lyrics / progress / link / keyValue / divider，每个含 editor + renderer + data + schema + isEmpty |
| M2-3 媒体导入 | ✅ | `MediaPicker`（点击 / 拖拽 / 外链三入口）、`useMediaPicker`、尺寸与时长探测、超限拦截、失败就地提示 |
| M2-4 编辑器 UI | ✅ | `ModuleCard`（标题就地改写 + 模块选项 + 实时预览）、`ModulePicker`（按分类分组、规划中模块禁用）、`CompareCanvas`（行与模块拖拽 + 增删 + 行标题） |
| M2-5 属性面板 | ✅ | `InspectorPanel`（中轴间距、最大宽度、中轴光带、背景样式、密度） |
| M2-6 命令层扩展 | ✅ | `modules/reorder`（整格原子重排）、`row/add` 支持 `at`（插入即一步撤销） |
| M2-7 LRC 解析 | ✅ | `lib/lrc.ts`：多时间戳、多精度、元信息、异常容错 |
| M2-8 测试 | ✅ | 单测 **191 passed**；E2E **27 passed** |

**M2 期间发现并修复的真实缺陷**：

| # | 问题 | 根因 | 修复 |
| --- | --- | --- | --- |
| 1 | 模块注册表在运行时是空的 | `modules/index.ts` 是副作用注册文件，**从未被 import** | `main.ts` 显式 `import './modules'` 并加注释 |
| 2 | 模块卡片只有标题，没有编辑器与预览 | SFC 用 `import type { XData } from './index'` 回指注册文件，与 `index.ts → SFC` 的动态导入构成**循环依赖** | 约定：模块类型一律放 `data.ts`，`index.ts` 只管注册 |
| 3 | 动态导入 500 / `Failed to fetch dynamically imported module` | `defineProps<ModuleEditorProps>()` 的类型来自**路径别名**（`@/modules/types`），`@vue/compiler-sfc` 无法解析别名 | 改为相对导入 `../types` |
| 4 | `inspectorOpen` 未从 store 导出 | 定义了却忘了写进 return | 补上；并修正了"用 PowerShell 管道读 `$LASTEXITCODE`"造成的**假绿** |
| 5 | **上传成功但预览始终加载失败（刷新后才正常）** | **模块的 editor / renderer 在 setup 时把 `props.module.data` 快照成了常量**。store 换掉模块对象（写入 assetId）后，`ModuleCard` 已拿到新对象（DOM 探针证实），但子组件仍读旧对象，于是界面永不更新 | 全部改为 `computed(() => props.module.data)` 响应式读取；并在 `CoverRenderer` 写明原因 |
| 6 | 输入时输入框只能打进一个字符 | 修复 #5 途中曾把"内容指纹"并入卡片 `:key` 强制重建，导致每敲一个字就重挂载 → 失焦 | 放弃该方案，改为 #5 的响应式读取；并新增 E2E 回归用例锁死焦点稳定性 |
| 7 | 插入行后按一次撤销只回退一半 | 组件里"先 addRow 再 moveRow"两次 dispatch → 两条历史记录 | 合并为一条带 `at` 的 `row/add` 命令 |

**这次排查的教训（写入规范）**：

1. **先建可观测性，再改代码。** 前 5 轮我在没有证据的情况下反复改实现、跑 E2E、看截图，浪费了大量轮次。
   正确做法是：把状态渲染成可读探针（`data-*` 属性）→ 用 E2E 精确读取 → 再动手。
   探针法最终一次定位：`cardAsset=b7ff3c47`（卡片收到了新对象）但 `coverAsset=none`（渲染器没有），
   这条对比直接指向"子组件快照"这个根因。
2. **探针本身也会改变被测对象。** 中途我用模板内联表达式写探针，其中使用了可选链 `?.`，
   导致 Vue 模板编译失败、整个画布不渲染，让我误以为"应用崩溃了"。探针要简单、只读、且写法受控。
3. **不能用 PowerShell 做含非 ASCII 文件的文本往返。** 本项目因此损坏过 13 个文件（编码错乱）。
   含中文的源文件一律用文件工具改写；并保留全量编码扫描作为兜底。

---
